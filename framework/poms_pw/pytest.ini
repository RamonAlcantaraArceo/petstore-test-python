[pytest]
# Run from this directory: uv run pytest  (or pytest directly)
# The conftest.py above wires poms/ onto sys.path automatically.
addopts = -v
markers =
    smoke: quick structural smoke tests (selector presence, class imports)
    functional: integration tests that navigate, wait for visibility, and interact with components
