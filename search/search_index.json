{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"petstore-test-python","text":"<p>A structured Python test repository supporting API, UI (Selenium + POM), integration, and end-to-end testing of a Petstore API (originally inspired by Swagger Petstore, but now running locally at <code>http://localhost:8000</code>) with a shared interface and fluent assertions.</p>"},{"location":"#whats-inside","title":"What's Inside","text":"Layer Technology Language Python 3.12 Package manager uv Testing pytest, pytest-asyncio, Allure Report 3 Test data Faker, factory_boy Coverage pytest-cov \u2192 Codecov UI automation Selenium + Page Object Model API client requests Type checking mypy Linting ruff + black Pre-commit ruff, black, mypy CI/CD GitHub Actions Docs MkDocs + Material"},{"location":"#getting-started","title":"Getting Started","text":"<p>See the Getting Started guide for installation and first steps.</p>"},{"location":"api-tests/","title":"API Tests","text":"<p>API tests send real HTTP requests to the Petstore REST API using the requests library.</p>"},{"location":"api-tests/#location","title":"Location","text":"<pre><code>tests/api/\n\u251c\u2500\u2500 conftest.py       # API-specific fixtures (new_pet, etc.)\n\u251c\u2500\u2500 test_login.py     # Authentication tests\n\u2514\u2500\u2500 test_pets.py      # Pet CRUD tests\n</code></pre>"},{"location":"api-tests/#running","title":"Running","text":"<pre><code>uv run pytest tests/api/ -m api -v\n</code></pre>"},{"location":"api-tests/#example-login","title":"Example: Login","text":"<pre><code>from framework.assertions import assert_that, assert_response\n\ndef test_login_sets_authenticated_state(api_client):\n    assert_that(api_client.is_logged_in()).is_false()\n\n    api_client.login(\"user1\", \"password1\")\n\n    assert_that(api_client.is_logged_in()).is_true()\n</code></pre>"},{"location":"api-tests/#example-pet-crud","title":"Example: Pet CRUD","text":"<pre><code>def test_add_pet_returns_created_pet(api_client):\n    pet = api_client.add_pet(name=\"Fido\", status=\"available\", photoUrls=[])\n\n    assert_that(pet).has_keys(\"id\", \"name\", \"status\")\n    assert_that(pet[\"name\"]).equals(\"Fido\")\n    assert_that(pet[\"status\"]).equals(\"available\")\n\n    api_client.delete_pet(pet[\"id\"])\n</code></pre>"},{"location":"api-tests/#raw-response-assertions","title":"Raw Response Assertions","text":"<pre><code>def test_get_nonexistent_pet(api_client):\n    response = api_client.raw_get(\"/pet/999999999999\")\n    assert_response(response).is_not_found()\n</code></pre>"},{"location":"api-tests/#api-client-reference","title":"API Client Reference","text":"<p>REST API client for the Swagger Petstore.</p> <p>Parameters:</p> Name Type Description Default <code>base_url</code> <code>str</code> <p>Root URL of the petstore API (default: public demo).</p> <code>DEFAULT_BASE_URL</code> <code>timeout</code> <code>int</code> <p>Request timeout in seconds.</p> <code>DEFAULT_TIMEOUT</code> <code>api_key</code> <code>str | None</code> <p>Optional API key sent in the <code>api_key</code> header.</p> <code>None</code> Source code in <code>framework/api_client.py</code> <pre><code>class PetstoreApiClient:\n    \"\"\"REST API client for the Swagger Petstore.\n\n    Parameters\n    ----------\n    base_url:\n        Root URL of the petstore API (default: public demo).\n    timeout:\n        Request timeout in seconds.\n    api_key:\n        Optional API key sent in the ``api_key`` header.\n    \"\"\"\n\n    def __init__(\n        self,\n        base_url: str = DEFAULT_BASE_URL,\n        timeout: int = DEFAULT_TIMEOUT,\n        api_key: str | None = None,\n    ) -&gt; None:\n        self._base_url = base_url.rstrip(\"/\")\n        self._timeout = timeout\n        self._session = requests.Session()\n        self._session.headers.update({\"Content-Type\": \"application/json\"})\n        if api_key:\n            self._session.headers.update({\"X-API-Key\": api_key})\n        self._logged_in = False\n        self._auth_token: str | None = None\n\n    # ------------------------------------------------------------------\n    # Internal helpers\n    # ------------------------------------------------------------------\n\n    def _url(self, path: str) -&gt; str:\n        return f\"{self._base_url}/{path.lstrip('/')}\"\n\n    def _request(\n        self,\n        method: str,\n        path: str,\n        **kwargs: Any,\n    ) -&gt; requests.Response:\n        url = self._url(path)\n        import json as _json\n\n        # Prepare request log\n        from urllib.parse import urlencode, urlparse\n\n        parsed_url = urlparse(url)\n        base_url = f\"{parsed_url.scheme}://{parsed_url.netloc}\"\n        path_and_query = parsed_url.path\n        if parsed_url.query:\n            path_and_query += f\"?{parsed_url.query}\"\n        # If params are present, append as query string\n        params = kwargs.get(\"params\")\n        if params:\n            # If there is already a query, append with &amp;\n            sep = \"&amp;\" if parsed_url.query else \"?\"\n            path_and_query += f\"{sep}{urlencode(params)}\"\n        summary = f\"Request: {base_url} {method.upper()} {path_and_query}\"\n\n        request_log = {\n            \"event\": \"request\",\n            \"method\": method.upper(),\n            \"url\": url,\n            \"headers\": dict(self._session.headers),\n            \"extra_headers\": kwargs.get(\"headers\"),\n            \"params\": params,\n            \"json\": kwargs.get(\"json\"),\n            \"data\": kwargs.get(\"data\"),\n        }\n        pretty_request = _json.dumps(\n            request_log, default=str, ensure_ascii=False, indent=2\n        )\n        indented_request = \"\\n  \" + pretty_request.replace(\"\\n\", \"\\n  \")\n        logger.debug(f\"{summary}{indented_request}\")\n\n        response = self._session.request(\n            method,\n            url,\n            timeout=self._timeout,\n            **kwargs,\n        )\n\n        # Prepare response log\n        # If the body is JSON, include as native object; else as string\n        try:\n            body_obj = response.json()\n        except Exception:\n            body_obj = response.text\n\n        # Parse the URL to split base, path, and query\n        from urllib.parse import urlparse\n\n        parsed_url = urlparse(response.url)\n        base_url = f\"{parsed_url.scheme}://{parsed_url.netloc}\"\n        path_and_query = parsed_url.path\n        if parsed_url.query:\n            path_and_query += f\"?{parsed_url.query}\"\n\n        # Compose summary line\n        summary = f\"Response: {base_url} {method.upper()} {path_and_query} [{response.status_code}]\"\n\n        response_log = {\n            \"event\": \"response\",\n            \"status_code\": response.status_code,\n            \"reason\": response.reason,\n            \"headers\": dict(response.headers),\n            \"body\": body_obj,\n            \"url\": response.url,\n        }\n        pretty_response = _json.dumps(\n            response_log, default=str, ensure_ascii=False, indent=2\n        )\n        indented_response = \"\\n  \" + pretty_response.replace(\"\\n\", \"\\n  \")\n        logger.debug(f\"{summary}{indented_response}\")\n        return response\n\n    # ------------------------------------------------------------------\n    # Auth\n    # ------------------------------------------------------------------\n\n    def login(self, username: str, password: str) -&gt; PetstoreApiClient:\n        \"\"\"Authenticate with the petstore API.\n\n        On success, stores the session token and sets ``is_logged_in() == True``.\n        \"\"\"\n        response = self._request(\n            \"GET\",\n            \"/user/login\",\n            params={\"username\": username, \"password\": password},\n        )\n        response.raise_for_status()\n        # The demo API returns a string token in the JSON message field\n        data = response.json()\n        token = data.get(\"message\", \"\")\n        if token:\n            self._auth_token = token\n            self._session.headers.update({\"Authorization\": f\"Bearer {token}\"})\n        self._logged_in = True\n        logger.info(\"Logged in as %s\", username)\n        return self\n\n    def logout(self) -&gt; PetstoreApiClient:\n        \"\"\"Log out the current session.\"\"\"\n        self._request(\"GET\", \"/user/logout\")\n        self._logged_in = False\n        self._auth_token = None\n        return self\n\n    def is_logged_in(self) -&gt; bool:\n        \"\"\"Return ``True`` when an active session exists.\"\"\"\n        return self._logged_in\n\n    # ------------------------------------------------------------------\n    # Pets\n    # ------------------------------------------------------------------\n\n    def add_pet(\n        self,\n        name: str,\n        status: str = \"available\",\n        **kwargs: Any,\n    ) -&gt; dict[str, Any]:\n        \"\"\"Create a new pet.\n\n        Parameters\n        ----------\n        name:    Pet name.\n        status:  ``available`` | ``pending`` | ``sold``.\n        kwargs:  Extra fields merged into the request body.\n        \"\"\"\n        payload: dict[str, Any] = {\n            \"name\": name,\n            \"status\": status,\n            \"photoUrls\": kwargs.pop(\"photoUrls\", []),\n            **kwargs,\n        }\n        response = self._request(\"POST\", \"/pet\", json=payload)\n        response.raise_for_status()\n        return dict(response.json())\n\n    def get_pet(self, pet_id: int) -&gt; dict[str, Any]:\n        \"\"\"Retrieve a pet by id.\"\"\"\n        response = self._request(\"GET\", f\"/pet/{pet_id}\")\n        response.raise_for_status()\n        return dict(response.json())\n\n    def update_pet(self, pet_id: int, **kwargs: Any) -&gt; dict[str, Any]:\n        \"\"\"Update an existing pet (full replacement).\"\"\"\n        existing = self.get_pet(pet_id)\n        payload = {**existing, **kwargs, \"id\": pet_id}\n        response = self._request(\"PUT\", \"/pet\", json=payload)\n        response.raise_for_status()\n        return dict(response.json())\n\n    def delete_pet(self, pet_id: int) -&gt; None:\n        \"\"\"Delete a pet by id.\"\"\"\n        response = self._request(\"DELETE\", f\"/pet/{pet_id}\")\n        response.raise_for_status()\n\n    def find_pets_by_status(self, status: str) -&gt; list[dict[str, Any]]:\n        \"\"\"Return a list of pets matching *status*.\"\"\"\n        response = self._request(\n            \"GET\",\n            \"/pet/findByStatus\",\n            params={\"status\": status},\n        )\n        response.raise_for_status()\n        return list(response.json())\n\n    # ------------------------------------------------------------------\n    # Users (convenience)\n    # ------------------------------------------------------------------\n\n    def create_user(\n        self, username: str, password: str, **kwargs: Any\n    ) -&gt; dict[str, Any]:\n        \"\"\"Create a new user account.\"\"\"\n        payload: dict[str, Any] = {\n            \"username\": username,\n            \"password\": password,\n            \"email\": kwargs.pop(\"email\", f\"{username}@example.com\"),\n            \"firstName\": kwargs.pop(\"firstName\", \"\"),\n            \"lastName\": kwargs.pop(\"lastName\", \"\"),\n            \"phone\": kwargs.pop(\"phone\", \"\"),\n            \"userStatus\": 1,\n            **kwargs,\n        }\n        response = self._request(\"POST\", \"/user\", json=payload)\n        response.raise_for_status()\n        return dict(response.json())\n\n    def get_user(self, username: str) -&gt; dict[str, Any]:\n        \"\"\"Retrieve a user by username.\"\"\"\n        response = self._request(\"GET\", f\"/user/{username}\")\n        response.raise_for_status()\n        return dict(response.json())\n\n    def delete_user(self, username: str) -&gt; None:\n        \"\"\"Delete a user by username.\"\"\"\n        response = self._request(\"DELETE\", f\"/user/{username}\")\n        response.raise_for_status()\n\n    # ------------------------------------------------------------------\n    # Raw request access (for advanced / negative tests)\n    # ------------------------------------------------------------------\n\n    def raw_get(self, path: str, **kwargs: Any) -&gt; requests.Response:\n        \"\"\"Make a raw GET request and return the :class:`requests.Response`.\"\"\"\n        return self._request(\"GET\", path, **kwargs)\n\n    def raw_post(self, path: str, **kwargs: Any) -&gt; requests.Response:\n        \"\"\"Make a raw POST request and return the :class:`requests.Response`.\"\"\"\n        return self._request(\"POST\", path, **kwargs)\n\n    # ------------------------------------------------------------------\n    # Lifecycle\n    # ------------------------------------------------------------------\n\n    def close(self) -&gt; None:\n        \"\"\"Close the underlying :class:`requests.Session`.\"\"\"\n        self._session.close()\n        logger.debug(\"Session closed\")\n</code></pre>"},{"location":"api-tests/#framework.api_client.PetstoreApiClient.add_pet","title":"<code>add_pet(name, status='available', **kwargs)</code>","text":"<p>Create a new pet.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> required <code>status</code> <code>str</code> <code>'available'</code> <code>kwargs</code> <code>Any</code> <code>{}</code> Source code in <code>framework/api_client.py</code> <pre><code>def add_pet(\n    self,\n    name: str,\n    status: str = \"available\",\n    **kwargs: Any,\n) -&gt; dict[str, Any]:\n    \"\"\"Create a new pet.\n\n    Parameters\n    ----------\n    name:    Pet name.\n    status:  ``available`` | ``pending`` | ``sold``.\n    kwargs:  Extra fields merged into the request body.\n    \"\"\"\n    payload: dict[str, Any] = {\n        \"name\": name,\n        \"status\": status,\n        \"photoUrls\": kwargs.pop(\"photoUrls\", []),\n        **kwargs,\n    }\n    response = self._request(\"POST\", \"/pet\", json=payload)\n    response.raise_for_status()\n    return dict(response.json())\n</code></pre>"},{"location":"api-tests/#framework.api_client.PetstoreApiClient.close","title":"<code>close()</code>","text":"<p>Close the underlying :class:<code>requests.Session</code>.</p> Source code in <code>framework/api_client.py</code> <pre><code>def close(self) -&gt; None:\n    \"\"\"Close the underlying :class:`requests.Session`.\"\"\"\n    self._session.close()\n    logger.debug(\"Session closed\")\n</code></pre>"},{"location":"api-tests/#framework.api_client.PetstoreApiClient.create_user","title":"<code>create_user(username, password, **kwargs)</code>","text":"<p>Create a new user account.</p> Source code in <code>framework/api_client.py</code> <pre><code>def create_user(\n    self, username: str, password: str, **kwargs: Any\n) -&gt; dict[str, Any]:\n    \"\"\"Create a new user account.\"\"\"\n    payload: dict[str, Any] = {\n        \"username\": username,\n        \"password\": password,\n        \"email\": kwargs.pop(\"email\", f\"{username}@example.com\"),\n        \"firstName\": kwargs.pop(\"firstName\", \"\"),\n        \"lastName\": kwargs.pop(\"lastName\", \"\"),\n        \"phone\": kwargs.pop(\"phone\", \"\"),\n        \"userStatus\": 1,\n        **kwargs,\n    }\n    response = self._request(\"POST\", \"/user\", json=payload)\n    response.raise_for_status()\n    return dict(response.json())\n</code></pre>"},{"location":"api-tests/#framework.api_client.PetstoreApiClient.delete_pet","title":"<code>delete_pet(pet_id)</code>","text":"<p>Delete a pet by id.</p> Source code in <code>framework/api_client.py</code> <pre><code>def delete_pet(self, pet_id: int) -&gt; None:\n    \"\"\"Delete a pet by id.\"\"\"\n    response = self._request(\"DELETE\", f\"/pet/{pet_id}\")\n    response.raise_for_status()\n</code></pre>"},{"location":"api-tests/#framework.api_client.PetstoreApiClient.delete_user","title":"<code>delete_user(username)</code>","text":"<p>Delete a user by username.</p> Source code in <code>framework/api_client.py</code> <pre><code>def delete_user(self, username: str) -&gt; None:\n    \"\"\"Delete a user by username.\"\"\"\n    response = self._request(\"DELETE\", f\"/user/{username}\")\n    response.raise_for_status()\n</code></pre>"},{"location":"api-tests/#framework.api_client.PetstoreApiClient.find_pets_by_status","title":"<code>find_pets_by_status(status)</code>","text":"<p>Return a list of pets matching status.</p> Source code in <code>framework/api_client.py</code> <pre><code>def find_pets_by_status(self, status: str) -&gt; list[dict[str, Any]]:\n    \"\"\"Return a list of pets matching *status*.\"\"\"\n    response = self._request(\n        \"GET\",\n        \"/pet/findByStatus\",\n        params={\"status\": status},\n    )\n    response.raise_for_status()\n    return list(response.json())\n</code></pre>"},{"location":"api-tests/#framework.api_client.PetstoreApiClient.get_pet","title":"<code>get_pet(pet_id)</code>","text":"<p>Retrieve a pet by id.</p> Source code in <code>framework/api_client.py</code> <pre><code>def get_pet(self, pet_id: int) -&gt; dict[str, Any]:\n    \"\"\"Retrieve a pet by id.\"\"\"\n    response = self._request(\"GET\", f\"/pet/{pet_id}\")\n    response.raise_for_status()\n    return dict(response.json())\n</code></pre>"},{"location":"api-tests/#framework.api_client.PetstoreApiClient.get_user","title":"<code>get_user(username)</code>","text":"<p>Retrieve a user by username.</p> Source code in <code>framework/api_client.py</code> <pre><code>def get_user(self, username: str) -&gt; dict[str, Any]:\n    \"\"\"Retrieve a user by username.\"\"\"\n    response = self._request(\"GET\", f\"/user/{username}\")\n    response.raise_for_status()\n    return dict(response.json())\n</code></pre>"},{"location":"api-tests/#framework.api_client.PetstoreApiClient.is_logged_in","title":"<code>is_logged_in()</code>","text":"<p>Return <code>True</code> when an active session exists.</p> Source code in <code>framework/api_client.py</code> <pre><code>def is_logged_in(self) -&gt; bool:\n    \"\"\"Return ``True`` when an active session exists.\"\"\"\n    return self._logged_in\n</code></pre>"},{"location":"api-tests/#framework.api_client.PetstoreApiClient.login","title":"<code>login(username, password)</code>","text":"<p>Authenticate with the petstore API.</p> <p>On success, stores the session token and sets <code>is_logged_in() == True</code>.</p> Source code in <code>framework/api_client.py</code> <pre><code>def login(self, username: str, password: str) -&gt; PetstoreApiClient:\n    \"\"\"Authenticate with the petstore API.\n\n    On success, stores the session token and sets ``is_logged_in() == True``.\n    \"\"\"\n    response = self._request(\n        \"GET\",\n        \"/user/login\",\n        params={\"username\": username, \"password\": password},\n    )\n    response.raise_for_status()\n    # The demo API returns a string token in the JSON message field\n    data = response.json()\n    token = data.get(\"message\", \"\")\n    if token:\n        self._auth_token = token\n        self._session.headers.update({\"Authorization\": f\"Bearer {token}\"})\n    self._logged_in = True\n    logger.info(\"Logged in as %s\", username)\n    return self\n</code></pre>"},{"location":"api-tests/#framework.api_client.PetstoreApiClient.logout","title":"<code>logout()</code>","text":"<p>Log out the current session.</p> Source code in <code>framework/api_client.py</code> <pre><code>def logout(self) -&gt; PetstoreApiClient:\n    \"\"\"Log out the current session.\"\"\"\n    self._request(\"GET\", \"/user/logout\")\n    self._logged_in = False\n    self._auth_token = None\n    return self\n</code></pre>"},{"location":"api-tests/#framework.api_client.PetstoreApiClient.raw_get","title":"<code>raw_get(path, **kwargs)</code>","text":"<p>Make a raw GET request and return the :class:<code>requests.Response</code>.</p> Source code in <code>framework/api_client.py</code> <pre><code>def raw_get(self, path: str, **kwargs: Any) -&gt; requests.Response:\n    \"\"\"Make a raw GET request and return the :class:`requests.Response`.\"\"\"\n    return self._request(\"GET\", path, **kwargs)\n</code></pre>"},{"location":"api-tests/#framework.api_client.PetstoreApiClient.raw_post","title":"<code>raw_post(path, **kwargs)</code>","text":"<p>Make a raw POST request and return the :class:<code>requests.Response</code>.</p> Source code in <code>framework/api_client.py</code> <pre><code>def raw_post(self, path: str, **kwargs: Any) -&gt; requests.Response:\n    \"\"\"Make a raw POST request and return the :class:`requests.Response`.\"\"\"\n    return self._request(\"POST\", path, **kwargs)\n</code></pre>"},{"location":"api-tests/#framework.api_client.PetstoreApiClient.update_pet","title":"<code>update_pet(pet_id, **kwargs)</code>","text":"<p>Update an existing pet (full replacement).</p> Source code in <code>framework/api_client.py</code> <pre><code>def update_pet(self, pet_id: int, **kwargs: Any) -&gt; dict[str, Any]:\n    \"\"\"Update an existing pet (full replacement).\"\"\"\n    existing = self.get_pet(pet_id)\n    payload = {**existing, **kwargs, \"id\": pet_id}\n    response = self._request(\"PUT\", \"/pet\", json=payload)\n    response.raise_for_status()\n    return dict(response.json())\n</code></pre>"},{"location":"cicd/","title":"CI/CD","text":"<p>The repository uses GitHub Actions for automated testing and deployment.</p>"},{"location":"cicd/#workflows","title":"Workflows","text":""},{"location":"cicd/#ciyml-main-ci-pipeline","title":"<code>ci.yml</code> \u2013 Main CI Pipeline","text":"<p>Triggered on every push and pull request to <code>main</code>.</p> Job Description <code>lint</code> ruff, black (format check), mypy <code>api-tests</code> Runs <code>tests/api/</code> with coverage <code>integration-tests</code> Runs <code>tests/integration/</code> with coverage <code>e2e-tests</code> Runs <code>tests/e2e/</code> with coverage <code>ui-tests</code> Runs <code>tests/ui/</code> with headless Chrome <code>allure-report</code> Merges results and publishes to GitHub Pages"},{"location":"cicd/#docsyml-documentation-deployment","title":"<code>docs.yml</code> \u2013 Documentation Deployment","text":"<p>Triggered on pushes to <code>main</code> that touch <code>docs/</code> or <code>mkdocs.yml</code>. Builds MkDocs and deploys to the <code>gh-pages</code> branch.</p>"},{"location":"cicd/#secrets","title":"Secrets","text":"<p>Configure these in Settings \u2192 Secrets and variables \u2192 Actions:</p> Secret Description <code>CODECOV_TOKEN</code> Codecov upload token <code>GITHUB_TOKEN</code> Auto-provided by GitHub Actions"},{"location":"cicd/#coverage-reports","title":"Coverage Reports","text":"<p>Coverage is uploaded to Codecov for each test job, tagged with flags (<code>api</code>, <code>integration</code>, <code>e2e</code>).</p>"},{"location":"cicd/#allure-reports","title":"Allure Reports","text":"<p>Allure results are collected per suite job using <code>--alluredir</code> and uploaded as artifacts. After all suites complete (on <code>main</code> only) the <code>allure-report</code> job:</p> <ol> <li>Downloads and merges all <code>allure-results-*</code> artifacts.</li> <li>Restores historical trend data from the <code>gh-pages</code> branch    (<code>gh-pages/history/</code> \u2192 <code>allure-results/history/</code>).</li> <li>Runs Allure 3 CLI (<code>allure-commandline</code> npm package) to generate a    static HTML report:    <pre><code>allure generate --clean allure-results -o allure-report\n</code></pre></li> <li>Publishes the generated <code>allure-report/</code> directory to the <code>gh-pages</code> branch    via <code>peaceiris/actions-gh-pages</code>.</li> </ol> <p>The published report is available at:</p> <pre><code>https://&lt;user&gt;.github.io/petstore-test-python/\n</code></pre>"},{"location":"cicd/#local-allure-3-report-generation","title":"Local Allure 3 report generation","text":"<pre><code># Run any suite to collect results\nuv run pytest tests/api/ --alluredir=allure-results\n\n# Generate report (requires Node.js)\nnpx allure-commandline generate --clean allure-results -o allure-report\n\n# Open report\nnpx allure-commandline open allure-report\n</code></pre>"},{"location":"e2e-tests/","title":"End-to-End Tests","text":"<p>E2E tests exercise complete user journeys from start to finish, verifying that the system behaves correctly at the highest level.</p>"},{"location":"e2e-tests/#location","title":"Location","text":"<pre><code>tests/e2e/\n\u2514\u2500\u2500 test_pet_lifecycle.py    # Full adoption + user registration journeys\n</code></pre>"},{"location":"e2e-tests/#running","title":"Running","text":"<pre><code>uv run pytest tests/e2e/ -m e2e -v\n</code></pre>"},{"location":"e2e-tests/#example-pet-adoption-journey","title":"Example: Pet Adoption Journey","text":"<pre><code>def test_available_pet_can_be_marked_as_sold(api_client):\n    pet = api_client.add_pet(name=\"AdoptMe\", status=\"available\", photoUrls=[])\n\n    # Verify it appears in 'available' list\n    available = api_client.find_pets_by_status(\"available\")\n    assert_that(any(p[\"id\"] == pet[\"id\"] for p in available)).is_true()\n\n    # 'Purchase' the pet\n    api_client.update_pet(pet[\"id\"], status=\"sold\")\n\n    # Verify it moved to 'sold'\n    sold = api_client.find_pets_by_status(\"sold\")\n    assert_that(any(p[\"id\"] == pet[\"id\"] for p in sold)).is_true()\n\n    api_client.delete_pet(pet[\"id\"])\n</code></pre>"},{"location":"framework-reference/","title":"Framework Reference","text":""},{"location":"framework-reference/#fluent-assertions","title":"Fluent Assertions","text":"<p>Entry point for fluent assertions.</p> Example <p>::</p> <pre><code>assert_that(pet[\"name\"]).equals(\"Fido\")\nassert_that(pets).is_not_empty()\n</code></pre> Source code in <code>framework/assertions.py</code> <pre><code>def assert_that[T](value: T, description: str = \"\") -&gt; FluentAssertion[T]:\n    \"\"\"Entry point for fluent assertions.\n\n    Example\n    -------\n    ::\n\n        assert_that(pet[\"name\"]).equals(\"Fido\")\n        assert_that(pets).is_not_empty()\n    \"\"\"\n    return FluentAssertion(value, description)\n</code></pre> <p>Entry point for fluent HTTP response assertions.</p> Example <p>::</p> <pre><code>assert_response(resp).is_ok().json_has_key(\"id\")\n</code></pre> Source code in <code>framework/assertions.py</code> <pre><code>def assert_response(response: Any) -&gt; ResponseAssertion:\n    \"\"\"Entry point for fluent HTTP response assertions.\n\n    Example\n    -------\n    ::\n\n        assert_response(resp).is_ok().json_has_key(\"id\")\n    \"\"\"\n    return ResponseAssertion(response)\n</code></pre> <p>Chainable assertion wrapper.</p> <p>Instantiate via :func:<code>assert_that</code> rather than directly.</p> Source code in <code>framework/assertions.py</code> <pre><code>class FluentAssertion[T]:\n    \"\"\"Chainable assertion wrapper.\n\n    Instantiate via :func:`assert_that` rather than directly.\n    \"\"\"\n\n    def __init__(self, value: T, description: str = \"\") -&gt; None:\n        self._value = value\n        self._desc = description or repr(value)\n\n    # ------------------------------------------------------------------\n    # Generic\n    # ------------------------------------------------------------------\n\n    def equals(self, expected: Any) -&gt; FluentAssertion[T]:\n        \"\"\"Assert *value* == *expected*.\"\"\"\n        assert (\n            self._value == expected\n        ), f\"Expected {self._desc} to equal {expected!r}, but got {self._value!r}\"\n        return self\n\n    def not_equals(self, unexpected: Any) -&gt; FluentAssertion[T]:\n        \"\"\"Assert *value* != *unexpected*.\"\"\"\n        assert (\n            self._value != unexpected\n        ), f\"Expected {self._desc} not to equal {unexpected!r}\"\n        return self\n\n    def is_none(self) -&gt; FluentAssertion[T]:\n        \"\"\"Assert *value* is ``None``.\"\"\"\n        assert (\n            self._value is None\n        ), f\"Expected {self._desc} to be None, got {self._value!r}\"\n        return self\n\n    def is_not_none(self) -&gt; FluentAssertion[T]:\n        \"\"\"Assert *value* is not ``None``.\"\"\"\n        assert self._value is not None, f\"Expected {self._desc} not to be None\"\n        return self\n\n    def is_true(self) -&gt; FluentAssertion[T]:\n        \"\"\"Assert *value* is truthy.\"\"\"\n        assert self._value, f\"Expected {self._desc} to be truthy, got {self._value!r}\"\n        return self\n\n    def is_false(self) -&gt; FluentAssertion[T]:\n        \"\"\"Assert *value* is falsy.\"\"\"\n        assert (\n            not self._value\n        ), f\"Expected {self._desc} to be falsy, got {self._value!r}\"\n        return self\n\n    def is_instance_of(self, type_: type) -&gt; FluentAssertion[T]:\n        \"\"\"Assert *value* is an instance of *type_*.\"\"\"\n        assert isinstance(self._value, type_), (\n            f\"Expected {self._desc} to be instance of {type_.__name__}, \"\n            f\"got {type(self._value).__name__}\"\n        )\n        return self\n\n    # ------------------------------------------------------------------\n    # Comparison\n    # ------------------------------------------------------------------\n\n    def is_greater_than(self, other: Any) -&gt; FluentAssertion[T]:\n        assert (\n            self._value &gt; other\n        ), f\"Expected {self._desc} &gt; {other!r}, got {self._value!r}\"\n        return self\n\n    def is_less_than(self, other: Any) -&gt; FluentAssertion[T]:\n        assert (\n            self._value &lt; other\n        ), f\"Expected {self._desc} &lt; {other!r}, got {self._value!r}\"\n        return self\n\n    def is_greater_than_or_equal_to(self, other: Any) -&gt; FluentAssertion[T]:\n        assert (\n            self._value &gt;= other\n        ), f\"Expected {self._desc} &gt;= {other!r}, got {self._value!r}\"\n        return self\n\n    def is_less_than_or_equal_to(self, other: Any) -&gt; FluentAssertion[T]:\n        assert (\n            self._value &lt;= other\n        ), f\"Expected {self._desc} &lt;= {other!r}, got {self._value!r}\"\n        return self\n\n    # ------------------------------------------------------------------\n    # String\n    # ------------------------------------------------------------------\n\n    def contains(self, substring: str) -&gt; FluentAssertion[T]:\n        \"\"\"Assert the string *value* contains *substring*.\"\"\"\n        assert isinstance(\n            self._value, str\n        ), f\"Expected a string, got {type(self._value)}\"\n        assert (\n            substring in self._value\n        ), f\"Expected {self._desc} to contain {substring!r}\"\n        return self\n\n    def starts_with(self, prefix: str) -&gt; FluentAssertion[T]:\n        assert isinstance(\n            self._value, str\n        ), f\"Expected a string, got {type(self._value)}\"\n        assert self._value.startswith(\n            prefix\n        ), f\"Expected {self._desc} to start with {prefix!r}\"\n        return self\n\n    def ends_with(self, suffix: str) -&gt; FluentAssertion[T]:\n        assert isinstance(\n            self._value, str\n        ), f\"Expected a string, got {type(self._value)}\"\n        assert self._value.endswith(\n            suffix\n        ), f\"Expected {self._desc} to end with {suffix!r}\"\n        return self\n\n    def matches_pattern(self, pattern: str) -&gt; FluentAssertion[T]:\n        \"\"\"Assert the string *value* matches the regex *pattern*.\"\"\"\n        __tracebackhide__ = True\n        import re\n\n        assert isinstance(\n            self._value, str\n        ), f\"Expected a string, got {type(self._value)}\"\n        assert re.search(\n            pattern, self._value\n        ), f\"Expected {self._desc} to match pattern {pattern!r}\"\n        return self\n\n    # ------------------------------------------------------------------\n    # Collections\n    # ------------------------------------------------------------------\n\n    def is_empty(self) -&gt; FluentAssertion[T]:\n        \"\"\"Assert the sequence / mapping is empty.\"\"\"\n        assert len(self._value) == 0, (  # type: ignore[arg-type]\n            f\"Expected {self._desc} to be empty, got length {len(self._value)}\"  # type: ignore[arg-type]\n        )\n        return self\n\n    def is_not_empty(self) -&gt; FluentAssertion[T]:\n        \"\"\"Assert the sequence / mapping is NOT empty.\"\"\"\n        assert len(self._value) &gt; 0, (  # type: ignore[arg-type]\n            f\"Expected {self._desc} not to be empty\"\n        )\n        return self\n\n    def has_length(self, length: int) -&gt; FluentAssertion[T]:\n        actual = len(self._value)  # type: ignore[arg-type]\n        assert (\n            actual == length\n        ), f\"Expected {self._desc} to have length {length}, got {actual}\"\n        return self\n\n    def has_length_greater_than(self, length: int) -&gt; FluentAssertion[T]:\n        actual = len(self._value)  # type: ignore[arg-type]\n        assert (\n            actual &gt; length\n        ), f\"Expected {self._desc} to have length &gt; {length}, got {actual}\"\n        return self\n\n    def contains_item(self, item: Any) -&gt; FluentAssertion[T]:\n        \"\"\"Assert *item* is in the collection.\"\"\"\n        assert item in self._value, (  # type: ignore[operator]\n            f\"Expected {self._desc} to contain {item!r}\"\n        )\n        return self\n\n    # ------------------------------------------------------------------\n    # Dict / mapping\n    # ------------------------------------------------------------------\n\n    def has_key(self, key: str) -&gt; FluentAssertion[T]:\n        \"\"\"Assert the dict *value* has *key*.\"\"\"\n        assert key in self._value, (  # type: ignore[operator]\n            f\"Expected {self._desc} to have key {key!r}\"\n        )\n        return self\n\n    def has_keys(self, *keys: str) -&gt; FluentAssertion[T]:\n        for key in keys:\n            self.has_key(key)\n        return self\n\n    def key_value_equals(self, key: str, expected: Any) -&gt; FluentAssertion[T]:\n        \"\"\"Assert ``value[key] == expected``.\"\"\"\n        self.has_key(key)\n        actual = self._value[key]  # type: ignore[index]\n        assert (\n            actual == expected\n        ), f\"Expected {self._desc}[{key!r}] == {expected!r}, got {actual!r}\"\n        return self\n</code></pre> <p>Fluent assertions tailored for <code>requests.Response</code> objects.</p> Source code in <code>framework/assertions.py</code> <pre><code>class ResponseAssertion:\n    \"\"\"Fluent assertions tailored for ``requests.Response`` objects.\"\"\"\n\n    def __init__(self, response: Any) -&gt; None:\n        self._response = response\n\n    def has_status(self, code: int) -&gt; ResponseAssertion:\n        actual = self._response.status_code\n        assert (\n            actual == code\n        ), f\"Expected HTTP {code}, got {actual}. Body: {self._response.text[:200]}\"\n        return self\n\n    def is_ok(self) -&gt; ResponseAssertion:\n        return self.has_status(200)\n\n    def is_created(self) -&gt; ResponseAssertion:\n        return self.has_status(201)\n\n    def is_client_error(self) -&gt; ResponseAssertion:\n        \"\"\"Assert response is a 4xx client error.\"\"\"\n        actual = self._response.status_code\n        assert (\n            400 &lt;= actual &lt; 500\n        ), f\"Expected HTTP 4xx client error, got {actual}. Body: {self._response.text[:200]}\"\n        return self\n\n    def is_unauthorized(self) -&gt; ResponseAssertion:\n        return self.has_status(401)\n\n    def is_not_found(self) -&gt; ResponseAssertion:\n        return self.has_status(404)\n\n    def is_server_error(self) -&gt; ResponseAssertion:\n        \"\"\"Assert response is a 500 server error.\"\"\"\n        actual = self._response.status_code\n        assert (\n            actual == 500\n        ), f\"Expected HTTP 500 server error, got {actual}. Body: {self._response.text[:200]}\"\n\n        return self\n\n    def body_contains(self, text: str) -&gt; ResponseAssertion:\n        assert text in self._response.text, (\n            f\"Expected response body to contain {text!r}. \"\n            f\"Actual body: {self._response.text[:300]}\"\n        )\n        return self\n\n    def json_has_key(self, key: str) -&gt; ResponseAssertion:\n        __tracebackhide__ = True\n        data = self._response.json()\n        assert (\n            key in data\n        ), f\"Expected JSON body to have key {key!r}. Got keys: {list(data)}\"\n        return self\n\n    def json_key_equals(self, key: str, expected: Any) -&gt; ResponseAssertion:\n        data = self._response.json()\n        assert (\n            data.get(key) == expected\n        ), f\"Expected JSON[{key!r}] == {expected!r}, got {data.get(key)!r}\"\n        return self\n</code></pre>"},{"location":"framework-reference/#framework.assertions.FluentAssertion.contains","title":"<code>contains(substring)</code>","text":"<p>Assert the string value contains substring.</p> Source code in <code>framework/assertions.py</code> <pre><code>def contains(self, substring: str) -&gt; FluentAssertion[T]:\n    \"\"\"Assert the string *value* contains *substring*.\"\"\"\n    assert isinstance(\n        self._value, str\n    ), f\"Expected a string, got {type(self._value)}\"\n    assert (\n        substring in self._value\n    ), f\"Expected {self._desc} to contain {substring!r}\"\n    return self\n</code></pre>"},{"location":"framework-reference/#framework.assertions.FluentAssertion.contains_item","title":"<code>contains_item(item)</code>","text":"<p>Assert item is in the collection.</p> Source code in <code>framework/assertions.py</code> <pre><code>def contains_item(self, item: Any) -&gt; FluentAssertion[T]:\n    \"\"\"Assert *item* is in the collection.\"\"\"\n    assert item in self._value, (  # type: ignore[operator]\n        f\"Expected {self._desc} to contain {item!r}\"\n    )\n    return self\n</code></pre>"},{"location":"framework-reference/#framework.assertions.FluentAssertion.equals","title":"<code>equals(expected)</code>","text":"<p>Assert value == expected.</p> Source code in <code>framework/assertions.py</code> <pre><code>def equals(self, expected: Any) -&gt; FluentAssertion[T]:\n    \"\"\"Assert *value* == *expected*.\"\"\"\n    assert (\n        self._value == expected\n    ), f\"Expected {self._desc} to equal {expected!r}, but got {self._value!r}\"\n    return self\n</code></pre>"},{"location":"framework-reference/#framework.assertions.FluentAssertion.has_key","title":"<code>has_key(key)</code>","text":"<p>Assert the dict value has key.</p> Source code in <code>framework/assertions.py</code> <pre><code>def has_key(self, key: str) -&gt; FluentAssertion[T]:\n    \"\"\"Assert the dict *value* has *key*.\"\"\"\n    assert key in self._value, (  # type: ignore[operator]\n        f\"Expected {self._desc} to have key {key!r}\"\n    )\n    return self\n</code></pre>"},{"location":"framework-reference/#framework.assertions.FluentAssertion.is_empty","title":"<code>is_empty()</code>","text":"<p>Assert the sequence / mapping is empty.</p> Source code in <code>framework/assertions.py</code> <pre><code>def is_empty(self) -&gt; FluentAssertion[T]:\n    \"\"\"Assert the sequence / mapping is empty.\"\"\"\n    assert len(self._value) == 0, (  # type: ignore[arg-type]\n        f\"Expected {self._desc} to be empty, got length {len(self._value)}\"  # type: ignore[arg-type]\n    )\n    return self\n</code></pre>"},{"location":"framework-reference/#framework.assertions.FluentAssertion.is_false","title":"<code>is_false()</code>","text":"<p>Assert value is falsy.</p> Source code in <code>framework/assertions.py</code> <pre><code>def is_false(self) -&gt; FluentAssertion[T]:\n    \"\"\"Assert *value* is falsy.\"\"\"\n    assert (\n        not self._value\n    ), f\"Expected {self._desc} to be falsy, got {self._value!r}\"\n    return self\n</code></pre>"},{"location":"framework-reference/#framework.assertions.FluentAssertion.is_instance_of","title":"<code>is_instance_of(type_)</code>","text":"<p>Assert value is an instance of type_.</p> Source code in <code>framework/assertions.py</code> <pre><code>def is_instance_of(self, type_: type) -&gt; FluentAssertion[T]:\n    \"\"\"Assert *value* is an instance of *type_*.\"\"\"\n    assert isinstance(self._value, type_), (\n        f\"Expected {self._desc} to be instance of {type_.__name__}, \"\n        f\"got {type(self._value).__name__}\"\n    )\n    return self\n</code></pre>"},{"location":"framework-reference/#framework.assertions.FluentAssertion.is_none","title":"<code>is_none()</code>","text":"<p>Assert value is <code>None</code>.</p> Source code in <code>framework/assertions.py</code> <pre><code>def is_none(self) -&gt; FluentAssertion[T]:\n    \"\"\"Assert *value* is ``None``.\"\"\"\n    assert (\n        self._value is None\n    ), f\"Expected {self._desc} to be None, got {self._value!r}\"\n    return self\n</code></pre>"},{"location":"framework-reference/#framework.assertions.FluentAssertion.is_not_empty","title":"<code>is_not_empty()</code>","text":"<p>Assert the sequence / mapping is NOT empty.</p> Source code in <code>framework/assertions.py</code> <pre><code>def is_not_empty(self) -&gt; FluentAssertion[T]:\n    \"\"\"Assert the sequence / mapping is NOT empty.\"\"\"\n    assert len(self._value) &gt; 0, (  # type: ignore[arg-type]\n        f\"Expected {self._desc} not to be empty\"\n    )\n    return self\n</code></pre>"},{"location":"framework-reference/#framework.assertions.FluentAssertion.is_not_none","title":"<code>is_not_none()</code>","text":"<p>Assert value is not <code>None</code>.</p> Source code in <code>framework/assertions.py</code> <pre><code>def is_not_none(self) -&gt; FluentAssertion[T]:\n    \"\"\"Assert *value* is not ``None``.\"\"\"\n    assert self._value is not None, f\"Expected {self._desc} not to be None\"\n    return self\n</code></pre>"},{"location":"framework-reference/#framework.assertions.FluentAssertion.is_true","title":"<code>is_true()</code>","text":"<p>Assert value is truthy.</p> Source code in <code>framework/assertions.py</code> <pre><code>def is_true(self) -&gt; FluentAssertion[T]:\n    \"\"\"Assert *value* is truthy.\"\"\"\n    assert self._value, f\"Expected {self._desc} to be truthy, got {self._value!r}\"\n    return self\n</code></pre>"},{"location":"framework-reference/#framework.assertions.FluentAssertion.key_value_equals","title":"<code>key_value_equals(key, expected)</code>","text":"<p>Assert <code>value[key] == expected</code>.</p> Source code in <code>framework/assertions.py</code> <pre><code>def key_value_equals(self, key: str, expected: Any) -&gt; FluentAssertion[T]:\n    \"\"\"Assert ``value[key] == expected``.\"\"\"\n    self.has_key(key)\n    actual = self._value[key]  # type: ignore[index]\n    assert (\n        actual == expected\n    ), f\"Expected {self._desc}[{key!r}] == {expected!r}, got {actual!r}\"\n    return self\n</code></pre>"},{"location":"framework-reference/#framework.assertions.FluentAssertion.matches_pattern","title":"<code>matches_pattern(pattern)</code>","text":"<p>Assert the string value matches the regex pattern.</p> Source code in <code>framework/assertions.py</code> <pre><code>def matches_pattern(self, pattern: str) -&gt; FluentAssertion[T]:\n    \"\"\"Assert the string *value* matches the regex *pattern*.\"\"\"\n    __tracebackhide__ = True\n    import re\n\n    assert isinstance(\n        self._value, str\n    ), f\"Expected a string, got {type(self._value)}\"\n    assert re.search(\n        pattern, self._value\n    ), f\"Expected {self._desc} to match pattern {pattern!r}\"\n    return self\n</code></pre>"},{"location":"framework-reference/#framework.assertions.FluentAssertion.not_equals","title":"<code>not_equals(unexpected)</code>","text":"<p>Assert value != unexpected.</p> Source code in <code>framework/assertions.py</code> <pre><code>def not_equals(self, unexpected: Any) -&gt; FluentAssertion[T]:\n    \"\"\"Assert *value* != *unexpected*.\"\"\"\n    assert (\n        self._value != unexpected\n    ), f\"Expected {self._desc} not to equal {unexpected!r}\"\n    return self\n</code></pre>"},{"location":"framework-reference/#framework.assertions.ResponseAssertion.is_client_error","title":"<code>is_client_error()</code>","text":"<p>Assert response is a 4xx client error.</p> Source code in <code>framework/assertions.py</code> <pre><code>def is_client_error(self) -&gt; ResponseAssertion:\n    \"\"\"Assert response is a 4xx client error.\"\"\"\n    actual = self._response.status_code\n    assert (\n        400 &lt;= actual &lt; 500\n    ), f\"Expected HTTP 4xx client error, got {actual}. Body: {self._response.text[:200]}\"\n    return self\n</code></pre>"},{"location":"framework-reference/#framework.assertions.ResponseAssertion.is_server_error","title":"<code>is_server_error()</code>","text":"<p>Assert response is a 500 server error.</p> Source code in <code>framework/assertions.py</code> <pre><code>def is_server_error(self) -&gt; ResponseAssertion:\n    \"\"\"Assert response is a 500 server error.\"\"\"\n    actual = self._response.status_code\n    assert (\n        actual == 500\n    ), f\"Expected HTTP 500 server error, got {actual}. Body: {self._response.text[:200]}\"\n\n    return self\n</code></pre>"},{"location":"framework-reference/#common-interface","title":"Common Interface","text":"<p>               Bases: <code>Protocol</code></p> <p>Defines the common interface for API and UI petstore clients.</p> Source code in <code>framework/interfaces.py</code> <pre><code>@runtime_checkable\nclass PetstoreClientProtocol(Protocol):\n    \"\"\"Defines the common interface for API and UI petstore clients.\"\"\"\n\n    # ------------------------------------------------------------------\n    # Auth\n    # ------------------------------------------------------------------\n\n    def login(self, username: str, password: str) -&gt; PetstoreClientProtocol:\n        \"\"\"Log in with the given credentials.\n\n        Returns *self* to allow method chaining::\n\n            client.login(\"user\", \"pass\").get_pets()\n        \"\"\"\n        ...\n\n    def logout(self) -&gt; PetstoreClientProtocol:\n        \"\"\"Log out the current session.\"\"\"\n        ...\n\n    def is_logged_in(self) -&gt; bool:\n        \"\"\"Return ``True`` when the client has an active authenticated session.\"\"\"\n        ...\n\n    # ------------------------------------------------------------------\n    # Pets\n    # ------------------------------------------------------------------\n\n    def add_pet(\n        self, name: str, status: str = \"available\", **kwargs: Any\n    ) -&gt; dict[str, Any]:\n        \"\"\"Add a new pet and return a dict with at least ``{\"id\": ..., \"name\": ...}``.\"\"\"\n        ...\n\n    def get_pet(self, pet_id: int) -&gt; dict[str, Any]:\n        \"\"\"Retrieve a single pet by id.\"\"\"\n        ...\n\n    def update_pet(self, pet_id: int, **kwargs: Any) -&gt; dict[str, Any]:\n        \"\"\"Update an existing pet; returns the updated pet dict.\"\"\"\n        ...\n\n    def delete_pet(self, pet_id: int) -&gt; None:\n        \"\"\"Delete a pet by id.\"\"\"\n        ...\n\n    def find_pets_by_status(self, status: str) -&gt; list[dict[str, Any]]:\n        \"\"\"Return a list of pets matching the given *status*.\"\"\"\n        ...\n\n    # ------------------------------------------------------------------\n    # Teardown\n    # ------------------------------------------------------------------\n\n    def close(self) -&gt; None:\n        \"\"\"Release any resources held by the client (browser, sessions, \u2026).\"\"\"\n        ...\n</code></pre>"},{"location":"framework-reference/#framework.interfaces.PetstoreClientProtocol.add_pet","title":"<code>add_pet(name, status='available', **kwargs)</code>","text":"<p>Add a new pet and return a dict with at least <code>{\"id\": ..., \"name\": ...}</code>.</p> Source code in <code>framework/interfaces.py</code> <pre><code>def add_pet(\n    self, name: str, status: str = \"available\", **kwargs: Any\n) -&gt; dict[str, Any]:\n    \"\"\"Add a new pet and return a dict with at least ``{\"id\": ..., \"name\": ...}``.\"\"\"\n    ...\n</code></pre>"},{"location":"framework-reference/#framework.interfaces.PetstoreClientProtocol.close","title":"<code>close()</code>","text":"<p>Release any resources held by the client (browser, sessions, \u2026).</p> Source code in <code>framework/interfaces.py</code> <pre><code>def close(self) -&gt; None:\n    \"\"\"Release any resources held by the client (browser, sessions, \u2026).\"\"\"\n    ...\n</code></pre>"},{"location":"framework-reference/#framework.interfaces.PetstoreClientProtocol.delete_pet","title":"<code>delete_pet(pet_id)</code>","text":"<p>Delete a pet by id.</p> Source code in <code>framework/interfaces.py</code> <pre><code>def delete_pet(self, pet_id: int) -&gt; None:\n    \"\"\"Delete a pet by id.\"\"\"\n    ...\n</code></pre>"},{"location":"framework-reference/#framework.interfaces.PetstoreClientProtocol.find_pets_by_status","title":"<code>find_pets_by_status(status)</code>","text":"<p>Return a list of pets matching the given status.</p> Source code in <code>framework/interfaces.py</code> <pre><code>def find_pets_by_status(self, status: str) -&gt; list[dict[str, Any]]:\n    \"\"\"Return a list of pets matching the given *status*.\"\"\"\n    ...\n</code></pre>"},{"location":"framework-reference/#framework.interfaces.PetstoreClientProtocol.get_pet","title":"<code>get_pet(pet_id)</code>","text":"<p>Retrieve a single pet by id.</p> Source code in <code>framework/interfaces.py</code> <pre><code>def get_pet(self, pet_id: int) -&gt; dict[str, Any]:\n    \"\"\"Retrieve a single pet by id.\"\"\"\n    ...\n</code></pre>"},{"location":"framework-reference/#framework.interfaces.PetstoreClientProtocol.is_logged_in","title":"<code>is_logged_in()</code>","text":"<p>Return <code>True</code> when the client has an active authenticated session.</p> Source code in <code>framework/interfaces.py</code> <pre><code>def is_logged_in(self) -&gt; bool:\n    \"\"\"Return ``True`` when the client has an active authenticated session.\"\"\"\n    ...\n</code></pre>"},{"location":"framework-reference/#framework.interfaces.PetstoreClientProtocol.login","title":"<code>login(username, password)</code>","text":"<p>Log in with the given credentials.</p> <p>Returns self to allow method chaining::</p> <pre><code>client.login(\"user\", \"pass\").get_pets()\n</code></pre> Source code in <code>framework/interfaces.py</code> <pre><code>def login(self, username: str, password: str) -&gt; PetstoreClientProtocol:\n    \"\"\"Log in with the given credentials.\n\n    Returns *self* to allow method chaining::\n\n        client.login(\"user\", \"pass\").get_pets()\n    \"\"\"\n    ...\n</code></pre>"},{"location":"framework-reference/#framework.interfaces.PetstoreClientProtocol.logout","title":"<code>logout()</code>","text":"<p>Log out the current session.</p> Source code in <code>framework/interfaces.py</code> <pre><code>def logout(self) -&gt; PetstoreClientProtocol:\n    \"\"\"Log out the current session.\"\"\"\n    ...\n</code></pre>"},{"location":"framework-reference/#framework.interfaces.PetstoreClientProtocol.update_pet","title":"<code>update_pet(pet_id, **kwargs)</code>","text":"<p>Update an existing pet; returns the updated pet dict.</p> Source code in <code>framework/interfaces.py</code> <pre><code>def update_pet(self, pet_id: int, **kwargs: Any) -&gt; dict[str, Any]:\n    \"\"\"Update an existing pet; returns the updated pet dict.\"\"\"\n    ...\n</code></pre>"},{"location":"framework-reference/#api-client","title":"API Client","text":"<p>REST API client for the Swagger Petstore.</p> <p>Parameters:</p> Name Type Description Default <code>base_url</code> <code>str</code> <p>Root URL of the petstore API (default: public demo).</p> <code>DEFAULT_BASE_URL</code> <code>timeout</code> <code>int</code> <p>Request timeout in seconds.</p> <code>DEFAULT_TIMEOUT</code> <code>api_key</code> <code>str | None</code> <p>Optional API key sent in the <code>api_key</code> header.</p> <code>None</code> Source code in <code>framework/api_client.py</code> <pre><code>class PetstoreApiClient:\n    \"\"\"REST API client for the Swagger Petstore.\n\n    Parameters\n    ----------\n    base_url:\n        Root URL of the petstore API (default: public demo).\n    timeout:\n        Request timeout in seconds.\n    api_key:\n        Optional API key sent in the ``api_key`` header.\n    \"\"\"\n\n    def __init__(\n        self,\n        base_url: str = DEFAULT_BASE_URL,\n        timeout: int = DEFAULT_TIMEOUT,\n        api_key: str | None = None,\n    ) -&gt; None:\n        self._base_url = base_url.rstrip(\"/\")\n        self._timeout = timeout\n        self._session = requests.Session()\n        self._session.headers.update({\"Content-Type\": \"application/json\"})\n        if api_key:\n            self._session.headers.update({\"X-API-Key\": api_key})\n        self._logged_in = False\n        self._auth_token: str | None = None\n\n    # ------------------------------------------------------------------\n    # Internal helpers\n    # ------------------------------------------------------------------\n\n    def _url(self, path: str) -&gt; str:\n        return f\"{self._base_url}/{path.lstrip('/')}\"\n\n    def _request(\n        self,\n        method: str,\n        path: str,\n        **kwargs: Any,\n    ) -&gt; requests.Response:\n        url = self._url(path)\n        import json as _json\n\n        # Prepare request log\n        from urllib.parse import urlencode, urlparse\n\n        parsed_url = urlparse(url)\n        base_url = f\"{parsed_url.scheme}://{parsed_url.netloc}\"\n        path_and_query = parsed_url.path\n        if parsed_url.query:\n            path_and_query += f\"?{parsed_url.query}\"\n        # If params are present, append as query string\n        params = kwargs.get(\"params\")\n        if params:\n            # If there is already a query, append with &amp;\n            sep = \"&amp;\" if parsed_url.query else \"?\"\n            path_and_query += f\"{sep}{urlencode(params)}\"\n        summary = f\"Request: {base_url} {method.upper()} {path_and_query}\"\n\n        request_log = {\n            \"event\": \"request\",\n            \"method\": method.upper(),\n            \"url\": url,\n            \"headers\": dict(self._session.headers),\n            \"extra_headers\": kwargs.get(\"headers\"),\n            \"params\": params,\n            \"json\": kwargs.get(\"json\"),\n            \"data\": kwargs.get(\"data\"),\n        }\n        pretty_request = _json.dumps(\n            request_log, default=str, ensure_ascii=False, indent=2\n        )\n        indented_request = \"\\n  \" + pretty_request.replace(\"\\n\", \"\\n  \")\n        logger.debug(f\"{summary}{indented_request}\")\n\n        response = self._session.request(\n            method,\n            url,\n            timeout=self._timeout,\n            **kwargs,\n        )\n\n        # Prepare response log\n        # If the body is JSON, include as native object; else as string\n        try:\n            body_obj = response.json()\n        except Exception:\n            body_obj = response.text\n\n        # Parse the URL to split base, path, and query\n        from urllib.parse import urlparse\n\n        parsed_url = urlparse(response.url)\n        base_url = f\"{parsed_url.scheme}://{parsed_url.netloc}\"\n        path_and_query = parsed_url.path\n        if parsed_url.query:\n            path_and_query += f\"?{parsed_url.query}\"\n\n        # Compose summary line\n        summary = f\"Response: {base_url} {method.upper()} {path_and_query} [{response.status_code}]\"\n\n        response_log = {\n            \"event\": \"response\",\n            \"status_code\": response.status_code,\n            \"reason\": response.reason,\n            \"headers\": dict(response.headers),\n            \"body\": body_obj,\n            \"url\": response.url,\n        }\n        pretty_response = _json.dumps(\n            response_log, default=str, ensure_ascii=False, indent=2\n        )\n        indented_response = \"\\n  \" + pretty_response.replace(\"\\n\", \"\\n  \")\n        logger.debug(f\"{summary}{indented_response}\")\n        return response\n\n    # ------------------------------------------------------------------\n    # Auth\n    # ------------------------------------------------------------------\n\n    def login(self, username: str, password: str) -&gt; PetstoreApiClient:\n        \"\"\"Authenticate with the petstore API.\n\n        On success, stores the session token and sets ``is_logged_in() == True``.\n        \"\"\"\n        response = self._request(\n            \"GET\",\n            \"/user/login\",\n            params={\"username\": username, \"password\": password},\n        )\n        response.raise_for_status()\n        # The demo API returns a string token in the JSON message field\n        data = response.json()\n        token = data.get(\"message\", \"\")\n        if token:\n            self._auth_token = token\n            self._session.headers.update({\"Authorization\": f\"Bearer {token}\"})\n        self._logged_in = True\n        logger.info(\"Logged in as %s\", username)\n        return self\n\n    def logout(self) -&gt; PetstoreApiClient:\n        \"\"\"Log out the current session.\"\"\"\n        self._request(\"GET\", \"/user/logout\")\n        self._logged_in = False\n        self._auth_token = None\n        return self\n\n    def is_logged_in(self) -&gt; bool:\n        \"\"\"Return ``True`` when an active session exists.\"\"\"\n        return self._logged_in\n\n    # ------------------------------------------------------------------\n    # Pets\n    # ------------------------------------------------------------------\n\n    def add_pet(\n        self,\n        name: str,\n        status: str = \"available\",\n        **kwargs: Any,\n    ) -&gt; dict[str, Any]:\n        \"\"\"Create a new pet.\n\n        Parameters\n        ----------\n        name:    Pet name.\n        status:  ``available`` | ``pending`` | ``sold``.\n        kwargs:  Extra fields merged into the request body.\n        \"\"\"\n        payload: dict[str, Any] = {\n            \"name\": name,\n            \"status\": status,\n            \"photoUrls\": kwargs.pop(\"photoUrls\", []),\n            **kwargs,\n        }\n        response = self._request(\"POST\", \"/pet\", json=payload)\n        response.raise_for_status()\n        return dict(response.json())\n\n    def get_pet(self, pet_id: int) -&gt; dict[str, Any]:\n        \"\"\"Retrieve a pet by id.\"\"\"\n        response = self._request(\"GET\", f\"/pet/{pet_id}\")\n        response.raise_for_status()\n        return dict(response.json())\n\n    def update_pet(self, pet_id: int, **kwargs: Any) -&gt; dict[str, Any]:\n        \"\"\"Update an existing pet (full replacement).\"\"\"\n        existing = self.get_pet(pet_id)\n        payload = {**existing, **kwargs, \"id\": pet_id}\n        response = self._request(\"PUT\", \"/pet\", json=payload)\n        response.raise_for_status()\n        return dict(response.json())\n\n    def delete_pet(self, pet_id: int) -&gt; None:\n        \"\"\"Delete a pet by id.\"\"\"\n        response = self._request(\"DELETE\", f\"/pet/{pet_id}\")\n        response.raise_for_status()\n\n    def find_pets_by_status(self, status: str) -&gt; list[dict[str, Any]]:\n        \"\"\"Return a list of pets matching *status*.\"\"\"\n        response = self._request(\n            \"GET\",\n            \"/pet/findByStatus\",\n            params={\"status\": status},\n        )\n        response.raise_for_status()\n        return list(response.json())\n\n    # ------------------------------------------------------------------\n    # Users (convenience)\n    # ------------------------------------------------------------------\n\n    def create_user(\n        self, username: str, password: str, **kwargs: Any\n    ) -&gt; dict[str, Any]:\n        \"\"\"Create a new user account.\"\"\"\n        payload: dict[str, Any] = {\n            \"username\": username,\n            \"password\": password,\n            \"email\": kwargs.pop(\"email\", f\"{username}@example.com\"),\n            \"firstName\": kwargs.pop(\"firstName\", \"\"),\n            \"lastName\": kwargs.pop(\"lastName\", \"\"),\n            \"phone\": kwargs.pop(\"phone\", \"\"),\n            \"userStatus\": 1,\n            **kwargs,\n        }\n        response = self._request(\"POST\", \"/user\", json=payload)\n        response.raise_for_status()\n        return dict(response.json())\n\n    def get_user(self, username: str) -&gt; dict[str, Any]:\n        \"\"\"Retrieve a user by username.\"\"\"\n        response = self._request(\"GET\", f\"/user/{username}\")\n        response.raise_for_status()\n        return dict(response.json())\n\n    def delete_user(self, username: str) -&gt; None:\n        \"\"\"Delete a user by username.\"\"\"\n        response = self._request(\"DELETE\", f\"/user/{username}\")\n        response.raise_for_status()\n\n    # ------------------------------------------------------------------\n    # Raw request access (for advanced / negative tests)\n    # ------------------------------------------------------------------\n\n    def raw_get(self, path: str, **kwargs: Any) -&gt; requests.Response:\n        \"\"\"Make a raw GET request and return the :class:`requests.Response`.\"\"\"\n        return self._request(\"GET\", path, **kwargs)\n\n    def raw_post(self, path: str, **kwargs: Any) -&gt; requests.Response:\n        \"\"\"Make a raw POST request and return the :class:`requests.Response`.\"\"\"\n        return self._request(\"POST\", path, **kwargs)\n\n    # ------------------------------------------------------------------\n    # Lifecycle\n    # ------------------------------------------------------------------\n\n    def close(self) -&gt; None:\n        \"\"\"Close the underlying :class:`requests.Session`.\"\"\"\n        self._session.close()\n        logger.debug(\"Session closed\")\n</code></pre>"},{"location":"framework-reference/#framework.api_client.PetstoreApiClient.add_pet","title":"<code>add_pet(name, status='available', **kwargs)</code>","text":"<p>Create a new pet.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> required <code>status</code> <code>str</code> <code>'available'</code> <code>kwargs</code> <code>Any</code> <code>{}</code> Source code in <code>framework/api_client.py</code> <pre><code>def add_pet(\n    self,\n    name: str,\n    status: str = \"available\",\n    **kwargs: Any,\n) -&gt; dict[str, Any]:\n    \"\"\"Create a new pet.\n\n    Parameters\n    ----------\n    name:    Pet name.\n    status:  ``available`` | ``pending`` | ``sold``.\n    kwargs:  Extra fields merged into the request body.\n    \"\"\"\n    payload: dict[str, Any] = {\n        \"name\": name,\n        \"status\": status,\n        \"photoUrls\": kwargs.pop(\"photoUrls\", []),\n        **kwargs,\n    }\n    response = self._request(\"POST\", \"/pet\", json=payload)\n    response.raise_for_status()\n    return dict(response.json())\n</code></pre>"},{"location":"framework-reference/#framework.api_client.PetstoreApiClient.close","title":"<code>close()</code>","text":"<p>Close the underlying :class:<code>requests.Session</code>.</p> Source code in <code>framework/api_client.py</code> <pre><code>def close(self) -&gt; None:\n    \"\"\"Close the underlying :class:`requests.Session`.\"\"\"\n    self._session.close()\n    logger.debug(\"Session closed\")\n</code></pre>"},{"location":"framework-reference/#framework.api_client.PetstoreApiClient.create_user","title":"<code>create_user(username, password, **kwargs)</code>","text":"<p>Create a new user account.</p> Source code in <code>framework/api_client.py</code> <pre><code>def create_user(\n    self, username: str, password: str, **kwargs: Any\n) -&gt; dict[str, Any]:\n    \"\"\"Create a new user account.\"\"\"\n    payload: dict[str, Any] = {\n        \"username\": username,\n        \"password\": password,\n        \"email\": kwargs.pop(\"email\", f\"{username}@example.com\"),\n        \"firstName\": kwargs.pop(\"firstName\", \"\"),\n        \"lastName\": kwargs.pop(\"lastName\", \"\"),\n        \"phone\": kwargs.pop(\"phone\", \"\"),\n        \"userStatus\": 1,\n        **kwargs,\n    }\n    response = self._request(\"POST\", \"/user\", json=payload)\n    response.raise_for_status()\n    return dict(response.json())\n</code></pre>"},{"location":"framework-reference/#framework.api_client.PetstoreApiClient.delete_pet","title":"<code>delete_pet(pet_id)</code>","text":"<p>Delete a pet by id.</p> Source code in <code>framework/api_client.py</code> <pre><code>def delete_pet(self, pet_id: int) -&gt; None:\n    \"\"\"Delete a pet by id.\"\"\"\n    response = self._request(\"DELETE\", f\"/pet/{pet_id}\")\n    response.raise_for_status()\n</code></pre>"},{"location":"framework-reference/#framework.api_client.PetstoreApiClient.delete_user","title":"<code>delete_user(username)</code>","text":"<p>Delete a user by username.</p> Source code in <code>framework/api_client.py</code> <pre><code>def delete_user(self, username: str) -&gt; None:\n    \"\"\"Delete a user by username.\"\"\"\n    response = self._request(\"DELETE\", f\"/user/{username}\")\n    response.raise_for_status()\n</code></pre>"},{"location":"framework-reference/#framework.api_client.PetstoreApiClient.find_pets_by_status","title":"<code>find_pets_by_status(status)</code>","text":"<p>Return a list of pets matching status.</p> Source code in <code>framework/api_client.py</code> <pre><code>def find_pets_by_status(self, status: str) -&gt; list[dict[str, Any]]:\n    \"\"\"Return a list of pets matching *status*.\"\"\"\n    response = self._request(\n        \"GET\",\n        \"/pet/findByStatus\",\n        params={\"status\": status},\n    )\n    response.raise_for_status()\n    return list(response.json())\n</code></pre>"},{"location":"framework-reference/#framework.api_client.PetstoreApiClient.get_pet","title":"<code>get_pet(pet_id)</code>","text":"<p>Retrieve a pet by id.</p> Source code in <code>framework/api_client.py</code> <pre><code>def get_pet(self, pet_id: int) -&gt; dict[str, Any]:\n    \"\"\"Retrieve a pet by id.\"\"\"\n    response = self._request(\"GET\", f\"/pet/{pet_id}\")\n    response.raise_for_status()\n    return dict(response.json())\n</code></pre>"},{"location":"framework-reference/#framework.api_client.PetstoreApiClient.get_user","title":"<code>get_user(username)</code>","text":"<p>Retrieve a user by username.</p> Source code in <code>framework/api_client.py</code> <pre><code>def get_user(self, username: str) -&gt; dict[str, Any]:\n    \"\"\"Retrieve a user by username.\"\"\"\n    response = self._request(\"GET\", f\"/user/{username}\")\n    response.raise_for_status()\n    return dict(response.json())\n</code></pre>"},{"location":"framework-reference/#framework.api_client.PetstoreApiClient.is_logged_in","title":"<code>is_logged_in()</code>","text":"<p>Return <code>True</code> when an active session exists.</p> Source code in <code>framework/api_client.py</code> <pre><code>def is_logged_in(self) -&gt; bool:\n    \"\"\"Return ``True`` when an active session exists.\"\"\"\n    return self._logged_in\n</code></pre>"},{"location":"framework-reference/#framework.api_client.PetstoreApiClient.login","title":"<code>login(username, password)</code>","text":"<p>Authenticate with the petstore API.</p> <p>On success, stores the session token and sets <code>is_logged_in() == True</code>.</p> Source code in <code>framework/api_client.py</code> <pre><code>def login(self, username: str, password: str) -&gt; PetstoreApiClient:\n    \"\"\"Authenticate with the petstore API.\n\n    On success, stores the session token and sets ``is_logged_in() == True``.\n    \"\"\"\n    response = self._request(\n        \"GET\",\n        \"/user/login\",\n        params={\"username\": username, \"password\": password},\n    )\n    response.raise_for_status()\n    # The demo API returns a string token in the JSON message field\n    data = response.json()\n    token = data.get(\"message\", \"\")\n    if token:\n        self._auth_token = token\n        self._session.headers.update({\"Authorization\": f\"Bearer {token}\"})\n    self._logged_in = True\n    logger.info(\"Logged in as %s\", username)\n    return self\n</code></pre>"},{"location":"framework-reference/#framework.api_client.PetstoreApiClient.logout","title":"<code>logout()</code>","text":"<p>Log out the current session.</p> Source code in <code>framework/api_client.py</code> <pre><code>def logout(self) -&gt; PetstoreApiClient:\n    \"\"\"Log out the current session.\"\"\"\n    self._request(\"GET\", \"/user/logout\")\n    self._logged_in = False\n    self._auth_token = None\n    return self\n</code></pre>"},{"location":"framework-reference/#framework.api_client.PetstoreApiClient.raw_get","title":"<code>raw_get(path, **kwargs)</code>","text":"<p>Make a raw GET request and return the :class:<code>requests.Response</code>.</p> Source code in <code>framework/api_client.py</code> <pre><code>def raw_get(self, path: str, **kwargs: Any) -&gt; requests.Response:\n    \"\"\"Make a raw GET request and return the :class:`requests.Response`.\"\"\"\n    return self._request(\"GET\", path, **kwargs)\n</code></pre>"},{"location":"framework-reference/#framework.api_client.PetstoreApiClient.raw_post","title":"<code>raw_post(path, **kwargs)</code>","text":"<p>Make a raw POST request and return the :class:<code>requests.Response</code>.</p> Source code in <code>framework/api_client.py</code> <pre><code>def raw_post(self, path: str, **kwargs: Any) -&gt; requests.Response:\n    \"\"\"Make a raw POST request and return the :class:`requests.Response`.\"\"\"\n    return self._request(\"POST\", path, **kwargs)\n</code></pre>"},{"location":"framework-reference/#framework.api_client.PetstoreApiClient.update_pet","title":"<code>update_pet(pet_id, **kwargs)</code>","text":"<p>Update an existing pet (full replacement).</p> Source code in <code>framework/api_client.py</code> <pre><code>def update_pet(self, pet_id: int, **kwargs: Any) -&gt; dict[str, Any]:\n    \"\"\"Update an existing pet (full replacement).\"\"\"\n    existing = self.get_pet(pet_id)\n    payload = {**existing, **kwargs, \"id\": pet_id}\n    response = self._request(\"PUT\", \"/pet\", json=payload)\n    response.raise_for_status()\n    return dict(response.json())\n</code></pre>"},{"location":"framework-reference/#ui-client","title":"UI Client","text":"<p>Browser-based client for the Petstore web UI.</p> <p>Implements the same interface as :class:<code>~framework.api_client.PetstoreApiClient</code> so tests can be parameterised across both implementations.</p> <p>Parameters:</p> Name Type Description Default <code>base_url</code> <code>str</code> <p>Root URL of the web application.</p> <code>DEFAULT_UI_BASE_URL</code> <code>headless</code> <code>bool</code> <p>Run Chrome without a visible window (default: <code>True</code>).</p> <code>True</code> <code>driver</code> <code>Remote | None</code> <p>Provide a pre-configured WebDriver (e.g. for testing the client itself). When given, headless is ignored.</p> <code>None</code> Source code in <code>framework/ui_client.py</code> <pre><code>class PetstoreUiClient:\n    \"\"\"Browser-based client for the Petstore web UI.\n\n    Implements the same interface as :class:`~framework.api_client.PetstoreApiClient`\n    so tests can be parameterised across both implementations.\n\n    Parameters\n    ----------\n    base_url:\n        Root URL of the web application.\n    headless:\n        Run Chrome without a visible window (default: ``True``).\n    driver:\n        Provide a pre-configured WebDriver (e.g. for testing the client\n        itself). When given, *headless* is ignored.\n    \"\"\"\n\n    def __init__(\n        self,\n        base_url: str = DEFAULT_UI_BASE_URL,\n        headless: bool = True,\n        driver: webdriver.Remote | None = None,\n    ) -&gt; None:\n        self._base_url = base_url.rstrip(\"/\")\n        self._driver = driver or _build_chrome_driver(headless=headless)\n        self._login_page = LoginPage(self._driver, base_url=self._base_url)\n        self._logged_in = False\n\n    # ------------------------------------------------------------------\n    # Auth\n    # ------------------------------------------------------------------\n\n    def login(self, username: str, password: str) -&gt; PetstoreUiClient:\n        \"\"\"Navigate to the login page and submit the form.\"\"\"\n        self._login_page.open().login(username, password)\n        self._logged_in = self._login_page.is_logged_in()\n        return self\n\n    def logout(self) -&gt; PetstoreUiClient:\n        \"\"\"Click the logout button.\"\"\"\n        self._login_page.click_logout()\n        self._logged_in = False\n        return self\n\n    def is_logged_in(self) -&gt; bool:\n        return self._logged_in\n\n    # ------------------------------------------------------------------\n    # Pets (stub \u2013 wire up to your real UI once the front-end exists)\n    # ------------------------------------------------------------------\n\n    def add_pet(\n        self, name: str, status: str = \"available\", **kwargs: Any\n    ) -&gt; dict[str, Any]:\n        raise NotImplementedError(\n            \"add_pet via UI is not yet implemented. \"\n            \"Use PetstoreApiClient for pet CRUD operations.\"\n        )\n\n    def get_pet(self, pet_id: int) -&gt; dict[str, Any]:\n        raise NotImplementedError(\"get_pet via UI is not yet implemented.\")\n\n    def update_pet(self, pet_id: int, **kwargs: Any) -&gt; dict[str, Any]:\n        raise NotImplementedError(\"update_pet via UI is not yet implemented.\")\n\n    def delete_pet(self, pet_id: int) -&gt; None:\n        raise NotImplementedError(\"delete_pet via UI is not yet implemented.\")\n\n    def find_pets_by_status(self, status: str) -&gt; list[dict[str, Any]]:\n        raise NotImplementedError(\"find_pets_by_status via UI is not yet implemented.\")\n\n    # ------------------------------------------------------------------\n    # Lifecycle\n    # ------------------------------------------------------------------\n\n    def close(self) -&gt; None:\n        \"\"\"Quit the browser.\"\"\"\n        try:\n            self._driver.quit()\n        except Exception as exc:\n            logger.warning(\"Exception while closing browser: %s\", exc)\n</code></pre>"},{"location":"framework-reference/#framework.ui_client.PetstoreUiClient.close","title":"<code>close()</code>","text":"<p>Quit the browser.</p> Source code in <code>framework/ui_client.py</code> <pre><code>def close(self) -&gt; None:\n    \"\"\"Quit the browser.\"\"\"\n    try:\n        self._driver.quit()\n    except Exception as exc:\n        logger.warning(\"Exception while closing browser: %s\", exc)\n</code></pre>"},{"location":"framework-reference/#framework.ui_client.PetstoreUiClient.login","title":"<code>login(username, password)</code>","text":"<p>Navigate to the login page and submit the form.</p> Source code in <code>framework/ui_client.py</code> <pre><code>def login(self, username: str, password: str) -&gt; PetstoreUiClient:\n    \"\"\"Navigate to the login page and submit the form.\"\"\"\n    self._login_page.open().login(username, password)\n    self._logged_in = self._login_page.is_logged_in()\n    return self\n</code></pre>"},{"location":"framework-reference/#framework.ui_client.PetstoreUiClient.logout","title":"<code>logout()</code>","text":"<p>Click the logout button.</p> Source code in <code>framework/ui_client.py</code> <pre><code>def logout(self) -&gt; PetstoreUiClient:\n    \"\"\"Click the logout button.\"\"\"\n    self._login_page.click_logout()\n    self._logged_in = False\n    return self\n</code></pre>"},{"location":"framework-reference/#test-data-factories","title":"Test-Data Factories","text":"<p>               Bases: <code>DictFactory</code></p> <p>Generate pet payloads compatible with the Petstore API.</p> Source code in <code>framework/factories.py</code> <pre><code>class PetFactory(factory.DictFactory):\n    \"\"\"Generate pet payloads compatible with the Petstore API.\"\"\"\n\n    id = factory.LazyFunction(lambda: fake.random_int(min=1_000_000, max=9_999_999))\n    name = factory.LazyFunction(fake.first_name)\n    photoUrls: list[str] = factory.LazyFunction(lambda: [fake.image_url()])\n    status = factory.Iterator([\"available\", \"pending\", \"sold\"])\n    category = factory.LazyFunction(\n        lambda: {\"id\": fake.random_int(1, 10), \"name\": fake.word()}\n    )\n    tags = factory.LazyFunction(\n        lambda: [{\"id\": fake.random_int(1, 100), \"name\": fake.word()}]\n    )\n</code></pre> <p>               Bases: <code>DictFactory</code></p> <p>Generate user payloads compatible with the Petstore API.</p> Source code in <code>framework/factories.py</code> <pre><code>class UserFactory(factory.DictFactory):\n    \"\"\"Generate user payloads compatible with the Petstore API.\"\"\"\n\n    id = factory.LazyFunction(lambda: fake.random_int(min=1_000_000, max=9_999_999))\n    username = factory.LazyFunction(\n        lambda: fake.user_name() + str(fake.random_int(1, 9999))\n    )\n    firstName = factory.LazyFunction(fake.first_name)\n    lastName = factory.LazyFunction(fake.last_name)\n    email = factory.LazyAttribute(lambda o: f\"{o.username}@example.com\")\n    password = factory.LazyFunction(lambda: fake.password(length=12))\n    phone = factory.LazyFunction(fake.phone_number)\n    userStatus = 1\n</code></pre>"},{"location":"getting-started/","title":"Getting Started","text":""},{"location":"getting-started/#prerequisites","title":"Prerequisites","text":"<ul> <li>Python 3.12 or later</li> <li>uv \u2013 fast Python package manager</li> </ul> <pre><code>curl -LsSf https://astral.sh/uv/install.sh | sh\n</code></pre>"},{"location":"getting-started/#clone-install","title":"Clone &amp; Install","text":"<pre><code>git clone https://github.com/RamonAlcantaraArceo/petstore-test-python.git\ncd petstore-test-python\nuv sync\n</code></pre> <p><code>uv sync</code> installs all development dependencies defined in <code>pyproject.toml</code>.</p>"},{"location":"getting-started/#running-tests","title":"Running Tests","text":"<pre><code># API tests only\nuv run pytest tests/api/ -m api -v\n\n# Integration tests\nuv run pytest tests/integration/ -m integration -v\n\n# E2E tests\nuv run pytest tests/e2e/ -m e2e -v\n\n# All non-UI, non-benchmark tests\nuv run pytest -m \"not ui and not performance\" -v\n\n# With HTML coverage report\nuv run pytest tests/api/ --cov=framework --cov-report=html\n</code></pre>"},{"location":"getting-started/#pre-commit-hooks","title":"Pre-commit Hooks","text":"<p>Install the hooks once after cloning:</p> <pre><code>uv run pre-commit install\n</code></pre> <p>Run against all files manually:</p> <pre><code>uv run pre-commit run --all-files\n</code></pre>"},{"location":"getting-started/#environment-configuration","title":"Environment Configuration","text":"Variable Default Description <code>PETSTORE_API_BASE_URL</code> <code>http://localhost:8000</code> API root URL <code>PETSTORE_UI_BASE_URL</code> <code>https://the-internet.herokuapp.com</code> UI root URL <code>RUN_UI_TESTS</code> <code>0</code> Set to <code>1</code> to enable Selenium tests <code>HEADLESS</code> <code>true</code> Browser headless mode"},{"location":"getting-started/#local-secrets-1password-cli","title":"Local Secrets (1Password CLI)","text":"<pre><code>op run --env-file=.env.local -- uv run pytest tests/api/ -v\n</code></pre> <p><code>.env.local</code> example (never commit this file):</p> <pre><code>PETSTORE_API_KEY=op://MyVault/petstore/api_key\nCODECOV_TOKEN=op://MyVault/codecov/token\n</code></pre>"},{"location":"integration-tests/","title":"Integration Tests","text":"<p>Integration tests verify that multiple components work together correctly, testing full data round-trips through the API stack.</p>"},{"location":"integration-tests/#location","title":"Location","text":"<pre><code>tests/integration/\n\u2514\u2500\u2500 test_pets.py     # Pet lifecycle + multi-component scenarios\n</code></pre>"},{"location":"integration-tests/#running","title":"Running","text":"<pre><code>uv run pytest tests/integration/ -m integration -v\n</code></pre>"},{"location":"integration-tests/#example","title":"Example","text":"<pre><code>def test_create_read_update_delete_pet(api_client):\n    pet = api_client.add_pet(name=\"IntegrationPet\", status=\"available\", photoUrls=[])\n\n    fetched = api_client.get_pet(pet[\"id\"])\n    assert_that(fetched[\"name\"]).equals(\"IntegrationPet\")\n\n    updated = api_client.update_pet(pet[\"id\"], status=\"pending\")\n    assert_that(updated[\"status\"]).equals(\"pending\")\n\n    api_client.delete_pet(pet[\"id\"])\n\n    response = api_client.raw_get(f\"/pet/{pet['id']}\")\n    assert_that(response.status_code).equals(404)\n</code></pre>"},{"location":"performance-tests/","title":"Performance Tests","text":"<p>Performance / benchmark tests use pytest-benchmark to measure the speed of key operations.</p> <p>Manual Only</p> <p>Performance tests are excluded from CI and should be run manually to avoid flaky results caused by CI runner variability.</p>"},{"location":"performance-tests/#location","title":"Location","text":"<pre><code>tests/performance/\n\u2514\u2500\u2500 test_benchmarks.py\n</code></pre>"},{"location":"performance-tests/#running","title":"Running","text":"<pre><code>uv run pytest tests/performance/ --benchmark-only\n</code></pre>"},{"location":"performance-tests/#comparing-runs","title":"Comparing Runs","text":"<pre><code># Save a baseline\nuv run pytest tests/performance/ --benchmark-only --benchmark-save=baseline\n\n# Compare against baseline\nuv run pytest tests/performance/ --benchmark-only --benchmark-compare=baseline\n</code></pre>"},{"location":"performance-tests/#example","title":"Example","text":"<pre><code>def test_find_pets_by_status_speed(benchmark, api_client):\n    result = benchmark(api_client.find_pets_by_status, \"available\")\n    assert isinstance(result, list)\n</code></pre>"},{"location":"ui-tests/","title":"UI Tests","text":"<p>UI tests automate a real browser using Selenium and the Page Object Model (POM) pattern to keep locators and interactions organised and reusable.</p>"},{"location":"ui-tests/#location","title":"Location","text":"<pre><code>tests/ui/\n\u2514\u2500\u2500 test_login.py    # Selenium login tests\n\nframework/pages/\n\u251c\u2500\u2500 base_page.py     # Base POM \u2013 common helpers\n\u251c\u2500\u2500 login_page.py    # Login page\n\u2514\u2500\u2500 pets_page.py     # Pets / swagger UI page\n</code></pre>"},{"location":"ui-tests/#enabling-ui-tests","title":"Enabling UI Tests","text":"<p>UI tests are disabled by default to avoid requiring a browser in all environments.</p> <pre><code>RUN_UI_TESTS=1 uv run pytest tests/ui/ -m ui -v\n</code></pre> <p>Set <code>HEADLESS=false</code> to watch the browser:</p> <pre><code>RUN_UI_TESTS=1 HEADLESS=false uv run pytest tests/ui/ -m ui -v\n</code></pre>"},{"location":"ui-tests/#example-test","title":"Example Test","text":"<pre><code>@skip_if_no_ui\nclass TestLoginUi:\n    def test_successful_login_shows_secure_area(self, browser):\n        self._page.login(\"tomsmith\", \"SuperSecretPassword!\")\n\n        assert_that(self._page.is_logged_in()).is_true()\n        assert_that(self._page.get_flash_message()).contains(\n            \"You logged into a secure area!\"\n        )\n</code></pre> <p>Notice the assertion style is identical to the API login test \u2014 this is the shared-interface pattern in action.</p>"},{"location":"ui-tests/#page-object-reference","title":"Page Object Reference","text":"<p>               Bases: <code>BasePage</code></p> <p>Page Object for the Login page.</p> Source code in <code>framework/pages/login_page.py</code> <pre><code>class LoginPage(BasePage):\n    \"\"\"Page Object for the Login page.\"\"\"\n\n    URL = \"/login\"\n\n    # ------------------------------------------------------------------\n    # Locators\n    # ------------------------------------------------------------------\n    _USERNAME_INPUT = (By.ID, \"username\")\n    _PASSWORD_INPUT = (By.ID, \"password\")\n    _SUBMIT_BUTTON = (By.CSS_SELECTOR, \"button[type='submit']\")\n    _FLASH_MESSAGE = (By.ID, \"flash\")\n    _LOGOUT_BUTTON = (By.CSS_SELECTOR, \"a.button.secondary.radius\")\n\n    # ------------------------------------------------------------------\n    # Actions\n    # ------------------------------------------------------------------\n\n    def fill_username(self, username: str) -&gt; LoginPage:\n        self.type_text(*self._USERNAME_INPUT, text=username)\n        return self\n\n    def fill_password(self, password: str) -&gt; LoginPage:\n        self.type_text(*self._PASSWORD_INPUT, text=password)\n        return self\n\n    def click_login(self) -&gt; LoginPage:\n        self.click(*self._SUBMIT_BUTTON)\n        return self\n\n    def login(self, username: str, password: str) -&gt; LoginPage:\n        \"\"\"Fill the form and submit it in one call.\"\"\"\n        return self.fill_username(username).fill_password(password).click_login()\n\n    def get_flash_message(self) -&gt; str:\n        \"\"\"Return the text of the flash / alert message.\"\"\"\n        try:\n            return self.wait_for_visible(*self._FLASH_MESSAGE).text\n        except Exception:\n            return \"\"\n\n    def click_logout(self) -&gt; LoginPage:\n        \"\"\"Click the logout button (only available after login).\"\"\"\n        self.click(*self._LOGOUT_BUTTON)\n        return self\n\n    # ------------------------------------------------------------------\n    # State queries\n    # ------------------------------------------------------------------\n\n    def is_logged_in(self) -&gt; bool:\n        \"\"\"Return True if a successful-login indicator is on the page.\"\"\"\n        message = self.get_flash_message()\n        return \"You logged into a secure area!\" in message\n\n    def is_login_failed(self) -&gt; bool:\n        \"\"\"Return True when an error message is displayed.\"\"\"\n        message = self.get_flash_message()\n        return (\n            \"Your username is invalid!\" in message\n            or \"Your password is invalid!\" in message\n        )\n</code></pre> <p>Thin Selenium wrapper shared by all page objects.</p> Source code in <code>framework/pages/base_page.py</code> <pre><code>class BasePage:\n    \"\"\"Thin Selenium wrapper shared by all page objects.\"\"\"\n\n    URL: str = \"\"  # Relative URL; subclasses should override\n\n    def __init__(self, driver: webdriver.Remote, base_url: str = \"\") -&gt; None:\n        self._driver = driver\n        self._base_url = base_url.rstrip(\"/\")\n        self._wait = WebDriverWait(driver, DEFAULT_TIMEOUT)\n\n    # ------------------------------------------------------------------\n    # Navigation\n    # ------------------------------------------------------------------\n\n    def open(self) -&gt; Self:\n        \"\"\"Navigate to the page's canonical URL.\"\"\"\n        url = f\"{self._base_url}{self.URL}\"\n        logger.debug(\"Navigating to %s\", url)\n        self._driver.get(url)\n        return self\n\n    def navigate_to(self, url: str) -&gt; Self:\n        \"\"\"Navigate to an arbitrary URL.\"\"\"\n        logger.debug(\"Navigating to %s\", url)\n        self._driver.get(url)\n        return self\n\n    @property\n    def title(self) -&gt; str:\n        \"\"\"Return the current page title.\"\"\"\n        return str(self._driver.title)\n\n    @property\n    def current_url(self) -&gt; str:\n        \"\"\"Return the current URL.\"\"\"\n        return str(self._driver.current_url)\n\n    # ------------------------------------------------------------------\n    # Element helpers\n    # ------------------------------------------------------------------\n\n    def find(self, by: str, value: str) -&gt; WebElement:\n        \"\"\"Find a single element (no explicit wait).\"\"\"\n        return cast(WebElement, self._driver.find_element(by, value))\n\n    def find_all(self, by: str, value: str) -&gt; list[WebElement]:\n        \"\"\"Find all matching elements (no explicit wait).\"\"\"\n        return cast(list[WebElement], self._driver.find_elements(by, value))\n\n    def wait_for_visible(\n        self, by: str, value: str, timeout: int = DEFAULT_TIMEOUT\n    ) -&gt; WebElement:\n        \"\"\"Wait until an element is visible and return it.\"\"\"\n        wait = WebDriverWait(self._driver, timeout)\n        result: WebElement = wait.until(EC.visibility_of_element_located((by, value)))\n        return result\n\n    def wait_for_clickable(\n        self, by: str, value: str, timeout: int = DEFAULT_TIMEOUT\n    ) -&gt; WebElement:\n        \"\"\"Wait until an element is clickable and return it.\"\"\"\n        wait = WebDriverWait(self._driver, timeout)\n        result: WebElement = wait.until(EC.element_to_be_clickable((by, value)))\n        return result\n\n    def wait_for_text(\n        self, by: str, value: str, text: str, timeout: int = DEFAULT_TIMEOUT\n    ) -&gt; bool:\n        \"\"\"Wait until an element contains *text*.\"\"\"\n        wait = WebDriverWait(self._driver, timeout)\n        try:\n            return bool(wait.until(EC.text_to_be_present_in_element((by, value), text)))\n        except TimeoutException:\n            return False\n\n    def wait_for_url_contains(\n        self, fragment: str, timeout: int = DEFAULT_TIMEOUT\n    ) -&gt; bool:\n        \"\"\"Wait until the current URL contains *fragment*.\"\"\"\n        wait = WebDriverWait(self._driver, timeout)\n        try:\n            return bool(wait.until(EC.url_contains(fragment)))\n        except TimeoutException:\n            return False\n\n    def is_element_present(self, by: str, value: str) -&gt; bool:\n        \"\"\"Return True if the element exists in the DOM.\"\"\"\n        return len(self.find_all(by, value)) &gt; 0\n\n    def click(self, by: str, value: str) -&gt; Self:\n        \"\"\"Wait for an element to be clickable then click it.\"\"\"\n        self.wait_for_clickable(by, value).click()\n        return self\n\n    def type_text(self, by: str, value: str, text: str, clear: bool = True) -&gt; Self:\n        \"\"\"Clear (optionally) then type *text* into the element.\"\"\"\n        element = self.wait_for_visible(by, value)\n        if clear:\n            element.clear()\n        element.send_keys(text)\n        return self\n\n    def get_text(self, by: str, value: str) -&gt; str:\n        \"\"\"Return the visible text of an element.\"\"\"\n        return str(self.wait_for_visible(by, value).text)\n\n    def get_attribute(self, by: str, value: str, attribute: str) -&gt; Any:\n        \"\"\"Return *attribute* of the first matching element.\"\"\"\n        return self.find(by, value).get_attribute(attribute)\n\n    def scroll_to(self, element: WebElement) -&gt; Self:\n        \"\"\"Scroll the element into view.\"\"\"\n        self._driver.execute_script(\"arguments[0].scrollIntoView(true);\", element)\n        return self\n\n    def take_screenshot(self, path: str) -&gt; None:\n        \"\"\"Save a screenshot to *path*.\"\"\"\n        self._driver.save_screenshot(path)\n</code></pre>"},{"location":"ui-tests/#framework.pages.login_page.LoginPage.click_logout","title":"<code>click_logout()</code>","text":"<p>Click the logout button (only available after login).</p> Source code in <code>framework/pages/login_page.py</code> <pre><code>def click_logout(self) -&gt; LoginPage:\n    \"\"\"Click the logout button (only available after login).\"\"\"\n    self.click(*self._LOGOUT_BUTTON)\n    return self\n</code></pre>"},{"location":"ui-tests/#framework.pages.login_page.LoginPage.get_flash_message","title":"<code>get_flash_message()</code>","text":"<p>Return the text of the flash / alert message.</p> Source code in <code>framework/pages/login_page.py</code> <pre><code>def get_flash_message(self) -&gt; str:\n    \"\"\"Return the text of the flash / alert message.\"\"\"\n    try:\n        return self.wait_for_visible(*self._FLASH_MESSAGE).text\n    except Exception:\n        return \"\"\n</code></pre>"},{"location":"ui-tests/#framework.pages.login_page.LoginPage.is_logged_in","title":"<code>is_logged_in()</code>","text":"<p>Return True if a successful-login indicator is on the page.</p> Source code in <code>framework/pages/login_page.py</code> <pre><code>def is_logged_in(self) -&gt; bool:\n    \"\"\"Return True if a successful-login indicator is on the page.\"\"\"\n    message = self.get_flash_message()\n    return \"You logged into a secure area!\" in message\n</code></pre>"},{"location":"ui-tests/#framework.pages.login_page.LoginPage.is_login_failed","title":"<code>is_login_failed()</code>","text":"<p>Return True when an error message is displayed.</p> Source code in <code>framework/pages/login_page.py</code> <pre><code>def is_login_failed(self) -&gt; bool:\n    \"\"\"Return True when an error message is displayed.\"\"\"\n    message = self.get_flash_message()\n    return (\n        \"Your username is invalid!\" in message\n        or \"Your password is invalid!\" in message\n    )\n</code></pre>"},{"location":"ui-tests/#framework.pages.login_page.LoginPage.login","title":"<code>login(username, password)</code>","text":"<p>Fill the form and submit it in one call.</p> Source code in <code>framework/pages/login_page.py</code> <pre><code>def login(self, username: str, password: str) -&gt; LoginPage:\n    \"\"\"Fill the form and submit it in one call.\"\"\"\n    return self.fill_username(username).fill_password(password).click_login()\n</code></pre>"},{"location":"ui-tests/#framework.pages.base_page.BasePage.current_url","title":"<code>current_url</code>  <code>property</code>","text":"<p>Return the current URL.</p>"},{"location":"ui-tests/#framework.pages.base_page.BasePage.title","title":"<code>title</code>  <code>property</code>","text":"<p>Return the current page title.</p>"},{"location":"ui-tests/#framework.pages.base_page.BasePage.click","title":"<code>click(by, value)</code>","text":"<p>Wait for an element to be clickable then click it.</p> Source code in <code>framework/pages/base_page.py</code> <pre><code>def click(self, by: str, value: str) -&gt; Self:\n    \"\"\"Wait for an element to be clickable then click it.\"\"\"\n    self.wait_for_clickable(by, value).click()\n    return self\n</code></pre>"},{"location":"ui-tests/#framework.pages.base_page.BasePage.find","title":"<code>find(by, value)</code>","text":"<p>Find a single element (no explicit wait).</p> Source code in <code>framework/pages/base_page.py</code> <pre><code>def find(self, by: str, value: str) -&gt; WebElement:\n    \"\"\"Find a single element (no explicit wait).\"\"\"\n    return cast(WebElement, self._driver.find_element(by, value))\n</code></pre>"},{"location":"ui-tests/#framework.pages.base_page.BasePage.find_all","title":"<code>find_all(by, value)</code>","text":"<p>Find all matching elements (no explicit wait).</p> Source code in <code>framework/pages/base_page.py</code> <pre><code>def find_all(self, by: str, value: str) -&gt; list[WebElement]:\n    \"\"\"Find all matching elements (no explicit wait).\"\"\"\n    return cast(list[WebElement], self._driver.find_elements(by, value))\n</code></pre>"},{"location":"ui-tests/#framework.pages.base_page.BasePage.get_attribute","title":"<code>get_attribute(by, value, attribute)</code>","text":"<p>Return attribute of the first matching element.</p> Source code in <code>framework/pages/base_page.py</code> <pre><code>def get_attribute(self, by: str, value: str, attribute: str) -&gt; Any:\n    \"\"\"Return *attribute* of the first matching element.\"\"\"\n    return self.find(by, value).get_attribute(attribute)\n</code></pre>"},{"location":"ui-tests/#framework.pages.base_page.BasePage.get_text","title":"<code>get_text(by, value)</code>","text":"<p>Return the visible text of an element.</p> Source code in <code>framework/pages/base_page.py</code> <pre><code>def get_text(self, by: str, value: str) -&gt; str:\n    \"\"\"Return the visible text of an element.\"\"\"\n    return str(self.wait_for_visible(by, value).text)\n</code></pre>"},{"location":"ui-tests/#framework.pages.base_page.BasePage.is_element_present","title":"<code>is_element_present(by, value)</code>","text":"<p>Return True if the element exists in the DOM.</p> Source code in <code>framework/pages/base_page.py</code> <pre><code>def is_element_present(self, by: str, value: str) -&gt; bool:\n    \"\"\"Return True if the element exists in the DOM.\"\"\"\n    return len(self.find_all(by, value)) &gt; 0\n</code></pre>"},{"location":"ui-tests/#framework.pages.base_page.BasePage.navigate_to","title":"<code>navigate_to(url)</code>","text":"<p>Navigate to an arbitrary URL.</p> Source code in <code>framework/pages/base_page.py</code> <pre><code>def navigate_to(self, url: str) -&gt; Self:\n    \"\"\"Navigate to an arbitrary URL.\"\"\"\n    logger.debug(\"Navigating to %s\", url)\n    self._driver.get(url)\n    return self\n</code></pre>"},{"location":"ui-tests/#framework.pages.base_page.BasePage.open","title":"<code>open()</code>","text":"<p>Navigate to the page's canonical URL.</p> Source code in <code>framework/pages/base_page.py</code> <pre><code>def open(self) -&gt; Self:\n    \"\"\"Navigate to the page's canonical URL.\"\"\"\n    url = f\"{self._base_url}{self.URL}\"\n    logger.debug(\"Navigating to %s\", url)\n    self._driver.get(url)\n    return self\n</code></pre>"},{"location":"ui-tests/#framework.pages.base_page.BasePage.scroll_to","title":"<code>scroll_to(element)</code>","text":"<p>Scroll the element into view.</p> Source code in <code>framework/pages/base_page.py</code> <pre><code>def scroll_to(self, element: WebElement) -&gt; Self:\n    \"\"\"Scroll the element into view.\"\"\"\n    self._driver.execute_script(\"arguments[0].scrollIntoView(true);\", element)\n    return self\n</code></pre>"},{"location":"ui-tests/#framework.pages.base_page.BasePage.take_screenshot","title":"<code>take_screenshot(path)</code>","text":"<p>Save a screenshot to path.</p> Source code in <code>framework/pages/base_page.py</code> <pre><code>def take_screenshot(self, path: str) -&gt; None:\n    \"\"\"Save a screenshot to *path*.\"\"\"\n    self._driver.save_screenshot(path)\n</code></pre>"},{"location":"ui-tests/#framework.pages.base_page.BasePage.type_text","title":"<code>type_text(by, value, text, clear=True)</code>","text":"<p>Clear (optionally) then type text into the element.</p> Source code in <code>framework/pages/base_page.py</code> <pre><code>def type_text(self, by: str, value: str, text: str, clear: bool = True) -&gt; Self:\n    \"\"\"Clear (optionally) then type *text* into the element.\"\"\"\n    element = self.wait_for_visible(by, value)\n    if clear:\n        element.clear()\n    element.send_keys(text)\n    return self\n</code></pre>"},{"location":"ui-tests/#framework.pages.base_page.BasePage.wait_for_clickable","title":"<code>wait_for_clickable(by, value, timeout=DEFAULT_TIMEOUT)</code>","text":"<p>Wait until an element is clickable and return it.</p> Source code in <code>framework/pages/base_page.py</code> <pre><code>def wait_for_clickable(\n    self, by: str, value: str, timeout: int = DEFAULT_TIMEOUT\n) -&gt; WebElement:\n    \"\"\"Wait until an element is clickable and return it.\"\"\"\n    wait = WebDriverWait(self._driver, timeout)\n    result: WebElement = wait.until(EC.element_to_be_clickable((by, value)))\n    return result\n</code></pre>"},{"location":"ui-tests/#framework.pages.base_page.BasePage.wait_for_text","title":"<code>wait_for_text(by, value, text, timeout=DEFAULT_TIMEOUT)</code>","text":"<p>Wait until an element contains text.</p> Source code in <code>framework/pages/base_page.py</code> <pre><code>def wait_for_text(\n    self, by: str, value: str, text: str, timeout: int = DEFAULT_TIMEOUT\n) -&gt; bool:\n    \"\"\"Wait until an element contains *text*.\"\"\"\n    wait = WebDriverWait(self._driver, timeout)\n    try:\n        return bool(wait.until(EC.text_to_be_present_in_element((by, value), text)))\n    except TimeoutException:\n        return False\n</code></pre>"},{"location":"ui-tests/#framework.pages.base_page.BasePage.wait_for_url_contains","title":"<code>wait_for_url_contains(fragment, timeout=DEFAULT_TIMEOUT)</code>","text":"<p>Wait until the current URL contains fragment.</p> Source code in <code>framework/pages/base_page.py</code> <pre><code>def wait_for_url_contains(\n    self, fragment: str, timeout: int = DEFAULT_TIMEOUT\n) -&gt; bool:\n    \"\"\"Wait until the current URL contains *fragment*.\"\"\"\n    wait = WebDriverWait(self._driver, timeout)\n    try:\n        return bool(wait.until(EC.url_contains(fragment)))\n    except TimeoutException:\n        return False\n</code></pre>"},{"location":"ui-tests/#framework.pages.base_page.BasePage.wait_for_visible","title":"<code>wait_for_visible(by, value, timeout=DEFAULT_TIMEOUT)</code>","text":"<p>Wait until an element is visible and return it.</p> Source code in <code>framework/pages/base_page.py</code> <pre><code>def wait_for_visible(\n    self, by: str, value: str, timeout: int = DEFAULT_TIMEOUT\n) -&gt; WebElement:\n    \"\"\"Wait until an element is visible and return it.\"\"\"\n    wait = WebDriverWait(self._driver, timeout)\n    result: WebElement = wait.until(EC.visibility_of_element_located((by, value)))\n    return result\n</code></pre>"}]}